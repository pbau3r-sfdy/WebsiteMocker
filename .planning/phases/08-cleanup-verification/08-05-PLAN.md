---
phase: 08-cleanup-verification
plan: 05
type: execute
wave: 3
depends_on: ["08-01", "08-04"]
files_modified:
  - _scripts/verify-phase-05.sh
  - .planning/phases/05-design-artifact-ingestion/05-VALIDATION.md
autonomous: true
requirements: [DEXP-06]

must_haves:
  truths:
    - "Operator can run `bash _scripts/verify-phase-05.sh` and get a green structural verification of the Phase 5 deliverables in under 5 seconds"
    - "A real artifact ingest has been executed end to end against ingest-artifact.mjs and its output inspected, not assumed"
    - "Phase 5 has a VALIDATION.md on disk recording the test infrastructure, sampling rate, per-task verification map, and manual-only verifications"
    - "The live ingest test left no residue in the working tree"
  artifacts:
    - path: "_scripts/verify-phase-05.sh"
      provides: "Phase 5 structural verification harness"
      contains: "ingest-artifact.mjs"
      min_lines: 60
    - path: ".planning/phases/05-design-artifact-ingestion/05-VALIDATION.md"
      provides: "Nyquist validation record for Phase 5"
      contains: "nyquist_compliant: true"
      min_lines: 50
  key_links:
    - from: ".planning/phases/05-design-artifact-ingestion/05-VALIDATION.md"
      to: "_scripts/verify-phase-05.sh"
      via: "cited quick-run and full-suite command"
      pattern: "verify-phase-05\\.sh"
    - from: "_scripts/verify-phase-05.sh"
      to: "_scripts/ingest-artifact.mjs"
      via: "structural assertions over the Phase 5 deliverable"
      pattern: "ingest-artifact\\.mjs"
    - from: ".planning/phases/05-design-artifact-ingestion/05-VALIDATION.md"
      to: "live ingest test evidence"
      via: "recorded command, output, and cleanup for the sfdy-alt-clean hero section run"
      pattern: "mode section"
---

<objective>
Give Phase 5 (Design Artifact Ingestion) a Nyquist validation record backed by a real end-to-end ingest run and a runnable automated harness.

Purpose: DEXP-06. Phase 5 has a VERIFICATION.md but no VALIDATION.md and no consolidated test artifact, so nothing re-proves the ingest pipeline on demand.
Output: `_scripts/verify-phase-05.sh`, a recorded live ingest test, and `.planning/phases/05-design-artifact-ingestion/05-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cleanup-verification/08-CONTEXT.md
@.planning/phases/08-cleanup-verification/08-PATTERNS.md
@AGENTS.md

<interfaces>
<!-- Contracts the executor needs. Do not re-derive these by exploring the codebase. -->

CLI surface of `_scripts/ingest-artifact.mjs` (1022 lines):
  node _scripts/ingest-artifact.mjs <slug> [--analyze] [--mode docs|full|section] [--section <name>]
       [--name <n>] [--format md] [--target-repo org/repo] [--commit] [--force] [--dry-run]

Top-level helper functions that the harness should assert exist:
  extractCSSVars, toPascalCase, walkTree, extractSections, extractStyleCSS, extractGoogleFontsLinks,
  extractImages, findHtmlFiles, warnMissingDocTokens, injectDocTokens, ghApiPutFile, runDocsMode,
  extractScopedCSS, rewriteLocalPaths, decodeBase64, convertLinkedStylesheets, toAstroComponent,
  writeSectionMode

Control-flow guarantees established by Phase 5 and re-verified in `05-VERIFICATION.md`:
  - `--analyze` writes nothing and prints a JSON object with keys: sections, artifactVars, existingVars,
    collisions, googleFontsLinks, images, base64Images
  - `--mode section` calls `writeSectionMode` then `process.exit(0)` — it writes only
    `sites/<slug>/src/components/<PascalName>.astro` and `sites/<slug>/public/images/<slug>/`, never `src/pages/`
  - before mode routing, the script checks `sites/<slug>/astro.config.mjs` for the SITE_URL/SITE_BASE pattern
    and injects it when absent (INGEST-02)
  - `extractScopedCSS` rewrites `url('/path')` to `url(`${b}/path`)`
  - `rewriteLocalPaths` rewrites local `src="/…"` and `href="/…"` to the `${b}/…` template-literal form
  - `convertLinkedStylesheets` inlines local `<link rel=stylesheet>` from the artifact dir
  - Nav/Footer overwrite protection guards existing site chrome in full mode

Live-test fixture already in the repo — a purpose-built minimal artifact:
  `_captures/sfdy-alt-clean/raw/artifact.html` (33 lines) containing
    :root { --bg: #ffffff; --accent: #ff0000; --text: #111111; }
    <nav class="nav">, <section class="hero" id="hero"> with <img src="/images/hero.jpg">, <footer class="footer">
  and a Google Fonts <link> to fonts.googleapis.com.

Why sfdy-alt-clean is the correct live-test target:
  - `sites/sfdy-alt-clean/astro.config.mjs` already has `process.env.SITE_BASE`, so the INGEST-02 check
    reports "confirmed" and writes nothing to the config
  - `sites/sfdy-alt-clean/src/components/` currently holds Beam, Footer, Nav, NewsCard, Newsletter —
    there is no `Hero.astro`, so `--section hero` cannot clobber existing chrome
  - `sites/sfdy-alt-clean/src/layouts/Layout.astro` defines `--accent: #00FB92`, which differs from the
    artifact's `--accent: #ff0000`, so `--analyze` must report a non-empty `collisions` array

Harness contract to reuse from `_scripts/verify-phase-04.sh`: `check(label, result)` incrementing PASS/FAIL,
`count_in FILE PATTERN` (fixed-string, comment-stripped), `count_re FILE PATTERN` (ERE, comment-stripped),
`count_re_js FILE PATTERN` (strips `//` comments), `set -euo pipefail`, and a boxed
`Results: $PASS passed, $FAIL failed` footer with `exit 0` / `exit 1`.

Format model for the output document: `.planning/phases/01-production-deploy-pipeline/01-VALIDATION.md`
(frontmatter keys phase/slug/status/nyquist_compliant/wave_0_complete/created; sections Test Infrastructure,
Sampling Rate, Per-Task Verification Map, Wave 0 Requirements, Manual-Only Verifications, Validation Sign-Off).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 5 structural verification harness</name>
  <files>_scripts/verify-phase-05.sh</files>
  <read_first>
    - _scripts/verify-phase-04.sh (harness contract: check(), count_in(), count_re(), count_re_js(), footer, exit codes — copy these helpers verbatim)
    - .planning/phases/05-design-artifact-ingestion/05-VERIFICATION.md (the Required Artifacts and Key Link tables define exactly what must be asserted)
    - .planning/phases/05-design-artifact-ingestion/05-01-SUMMARY.md, 05-02-SUMMARY.md, 05-03-SUMMARY.md (what each plan actually delivered, for the per-task map in Task 3)
    - _scripts/ingest-artifact.mjs (confirm the helper function names and the section-mode early exit before writing assertions against them)
    - .claude/skills/wm-ingest.md (112 lines — confirm the 7-step structure and the collision-gate wording)
  </read_first>
  <action>
Create `_scripts/verify-phase-05.sh`, executable, `set -euo pipefail`, usage
`bash _scripts/verify-phase-05.sh [01|02|03|all]` defaulting to `all`, matching the section-filter style of
`_scripts/verify-phase-04.sh`. Copy `check`, `count_in`, `count_re`, and `count_re_js` verbatim from
verify-phase-04.sh so comment lines are stripped before counting. Do not use a bare `grep -c` gate on an
unfiltered file.

Section `01` — script existence and parse integrity:
  - `_scripts/ingest-artifact.mjs` exists and `node --check _scripts/ingest-artifact.mjs` exits 0
  - each of these helper identifiers appears at least once: extractSections, extractStyleCSS,
    extractScopedCSS, extractGoogleFontsLinks, extractImages, decodeBase64, rewriteLocalPaths,
    convertLinkedStylesheets, toAstroComponent, writeSectionMode, toPascalCase, walkTree
  - the usage string advertises all three modes: assert the file contains `--mode docs|full|section`

Section `02` — mode-routing and safety guarantees:
  - `--analyze` JSON contract: the file contains all seven output keys — `sections`, `artifactVars`,
    `existingVars`, `collisions`, `googleFontsLinks`, `images`, `base64Images`
  - section-mode isolation: assert the file contains `if (modeArg === 'section')` and that
    `writeSectionMode` contains no `src', 'pages'` path construction. Implement this as a scoped check —
    extract the `writeSectionMode` function body with `sed -n '/^function writeSectionMode/,/^}/p'` and
    assert `count_re` over that extract for `'pages'` equals 0
  - INGEST-02 config guard: the file contains `SITE_URL` and `SITE_BASE` and the phrase
    `injected SITE_URL/SITE_BASE env var pattern`
  - CSS and path rewriting: the file contains `url(\`\${b}` and `${b}/` rewriting logic — assert
    `rewriteLocalPaths` and `extractScopedCSS` are both referenced from the write flow
  - Nav/Footer overwrite protection: the file contains the string `Nav/Footer overwrite protection`
  - mode guard rejects an unknown mode: the file contains
    `if (modeArg !== 'full' && modeArg !== 'section' && modeArg !== 'docs')`

Section `03` — operator skill surface:
  - `.claude/skills/wm-ingest.md` exists
  - it contains a `## Steps` heading and at least 7 numbered step markers
  - it contains the collision-gate instruction `Do NOT proceed to Step 5 until the operator explicitly types`
  - it references `_scripts/ingest-artifact.mjs`

Also add a behavioural check that runs the real script in a read-only mode and is therefore safe to re-run:
  - `node _scripts/ingest-artifact.mjs sfdy-alt-clean --analyze` exits 0 and its stdout parses as JSON with a
    non-empty `sections` array and a non-empty `collisions` array. Pipe stdout into
    `node -e` and assert with `process.exit(cond ? 0 : 1)`.
  - `node _scripts/ingest-artifact.mjs sfdy-alt-clean --mode section --section hero --dry-run` exits 0 and
    prints `would write Hero.astro`, and after it runs `sites/sfdy-alt-clean/src/components/Hero.astro`
    still does not exist (proves the dry-run guard)

End with the boxed `Results: $PASS passed, $FAIL failed` footer and `[ "$FAIL" -eq 0 ] && exit 0 || exit 1`.
Target total runtime under 5 seconds.
  </action>
  <verify>
    <automated>bash -n _scripts/verify-phase-05.sh &amp;&amp; chmod +x _scripts/verify-phase-05.sh &amp;&amp; time bash _scripts/verify-phase-05.sh all</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n _scripts/verify-phase-05.sh` exits 0
    - `test -x _scripts/verify-phase-05.sh` exits 0
    - `bash _scripts/verify-phase-05.sh all` exits 0
    - `bash _scripts/verify-phase-05.sh 01`, `02`, and `03` each exit 0
    - `bash _scripts/verify-phase-05.sh all` completes in under 5 seconds
    - `_scripts/verify-phase-05.sh` contains `count_in`, `count_re`, and `ingest-artifact.mjs`
    - `grep -c 'grep -c' _scripts/verify-phase-05.sh` returns 0
    - `test ! -f sites/sfdy-alt-clean/src/components/Hero.astro` exits 0 after the harness run (the dry-run assertion did not write)
  </acceptance_criteria>
  <done>`_scripts/verify-phase-05.sh` is executable, exits 0 for every section, runs in under 5 seconds, and includes two behavioural checks that invoke `ingest-artifact.mjs` in read-only modes.</done>
</task>

<task type="auto">
  <name>Task 2: Live end-to-end artifact ingest and cleanup</name>
  <files>sites/sfdy-alt-clean/src/components/Hero.astro</files>
  <read_first>
    - _captures/sfdy-alt-clean/raw/artifact.html (the 33-line test fixture: :root vars, .nav, #hero with a local img, .footer, Google Fonts link)
    - sites/sfdy-alt-clean/astro.config.mjs (confirm SITE_BASE env-var pattern is already present so INGEST-02 writes nothing)
    - sites/sfdy-alt-clean/src/layouts/Layout.astro (confirm --accent is #00FB92, which is what makes the collision non-empty)
    - _scripts/ingest-artifact.mjs lines 598-800 (extractScopedCSS, rewriteLocalPaths, toAstroComponent, writeSectionMode — the exact transformations to assert on in the output)
    - .planning/phases/05-design-artifact-ingestion/05-VERIFICATION.md (the four observable truths this live run must substantiate)
  </read_first>
  <action>
Per D-09, run the ingest pipeline for real against a real artifact and inspect the output. Record every
command, its exit code, and the relevant output lines — Task 3 turns these into the validation record.

Step 1 — analyze (no writes). Run
`node _scripts/ingest-artifact.mjs sfdy-alt-clean --analyze > /tmp/ingest-analyze.json` and confirm exit 0.
From the JSON, record: the `sections` array (expect entries for the `nav` element, the `hero` section, and
the `footer` element), the `collisions` array (expect at least `--accent` with existing `#00FB92` versus
artifact `#ff0000`), and `googleFontsLinks` (expect the fonts.googleapis.com Inter link). Confirm
`git status --porcelain` is unchanged by this step.

Step 2 — dry run. Run
`node _scripts/ingest-artifact.mjs sfdy-alt-clean --mode section --section hero --dry-run`, confirm exit 0,
record the `[dry] would write Hero.astro` line, and confirm
`test ! -f sites/sfdy-alt-clean/src/components/Hero.astro`.

Step 3 — real section-mode ingest. Run
`node _scripts/ingest-artifact.mjs sfdy-alt-clean --mode section --section hero`, confirm exit 0, and then
inspect `sites/sfdy-alt-clean/src/components/Hero.astro` and record concrete evidence for each of these:
  - the frontmatter declares the BASE_URL alias `const b = import.meta.env.BASE_URL.replace(/\/$/, '')`
    (present because the section markup contains a local asset reference)
  - the `<img>` src was rewritten from `/images/hero.jpg` to the `${b}/images/hero.jpg` template-literal form
  - the `<style>` block contains only the `.hero`-scoped rules (`.hero`, `.hero h1`, `.hero img`) and does NOT
    contain the global `:root`, `*`, `body`, `.nav`, or `.footer` rules
  - the component name is `Hero` (PascalCase of the section name)
Also confirm the safety guarantees: `git status --porcelain sites/sfdy-alt-clean/src/pages` reports no
changes, `git status --porcelain sites/sfdy-alt-clean/astro.config.mjs` reports no changes (INGEST-02
confirmed rather than injected), and the stdout ended with the manual-import instruction and
`No existing pages were modified.`

Step 4 — cleanup. Delete `sites/sfdy-alt-clean/src/components/Hero.astro`. Remove
`sites/sfdy-alt-clean/public/images/sfdy-alt-clean/` only if the ingest created it and it is empty — check
with `find sites/sfdy-alt-clean/public/images/sfdy-alt-clean -type f | wc -l` and use `rmdir` so a non-empty
directory cannot be destroyed. Then confirm `git status --porcelain sites/sfdy-alt-clean` produces no output.

Do NOT run `--mode full` on any site: full mode rewrites `src/pages/index.astro` and would destroy the live
sfdy-alt-clean homepage. Do NOT run `--mode docs --commit`: that is Phase 6 surface and would push to a
production repo. Do NOT pass `--target-repo` or `--commit` at any point in this plan.
  </action>
  <verify>
    <automated>node _scripts/ingest-artifact.mjs sfdy-alt-clean --analyze &gt; /tmp/ingest-analyze.json &amp;&amp; node -e "const j=require('/tmp/ingest-analyze.json'); if(!j.sections.length||!j.collisions.length) process.exit(1)" &amp;&amp; test -z "$(git status --porcelain sites/sfdy-alt-clean)"</automated>
  </verify>
  <acceptance_criteria>
    - `node _scripts/ingest-artifact.mjs sfdy-alt-clean --analyze` exits 0 and its stdout parses as JSON
    - the analyze JSON `sections` array has at least 3 entries and `collisions` has at least 1 entry naming `--accent`
    - `node _scripts/ingest-artifact.mjs sfdy-alt-clean --mode section --section hero --dry-run` exits 0 and prints `would write Hero.astro`
    - `node _scripts/ingest-artifact.mjs sfdy-alt-clean --mode section --section hero` exits 0 and prints `No existing pages were modified.`
    - during Step 3 the produced `Hero.astro` contained `import.meta.env.BASE_URL`, contained `${b}/images/hero.jpg`, contained `.hero`, and contained zero occurrences of `:root` and zero occurrences of `.footer`
    - after cleanup, `test ! -f sites/sfdy-alt-clean/src/components/Hero.astro` exits 0
    - after cleanup, `git status --porcelain sites/sfdy-alt-clean` produces no output
    - `git status --porcelain sites/sfdy-alt-clean/src/pages` produced no output at every step
    - `grep -c 'target-repo\|--commit' <the recorded command log>` shows the flags were never used
  </acceptance_criteria>
  <done>A real section-mode ingest ran against a real artifact, its output was inspected and shown to apply BASE_URL rewriting and CSS scoping, `src/pages` and `astro.config.mjs` were provably untouched, and the working tree is clean afterwards.</done>
</task>

<task type="auto">
  <name>Task 3: Write 05-VALIDATION.md</name>
  <files>.planning/phases/05-design-artifact-ingestion/05-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01-production-deploy-pipeline/01-VALIDATION.md (the exact frontmatter keys, section headings, table columns, and status legend to mirror)
    - .planning/phases/05-design-artifact-ingestion/05-01-SUMMARY.md, 05-02-SUMMARY.md, 05-03-SUMMARY.md (task inventory, requirement IDs, and threat refs for the per-task map)
    - .planning/phases/05-design-artifact-ingestion/05-VERIFICATION.md (the re-verification record and its three closed gaps)
    - .planning/phases/05-design-artifact-ingestion/05-01-PLAN.md, 05-02-PLAN.md, 05-03-PLAN.md (each task's verify block and threat IDs)
    - _scripts/verify-phase-05.sh (the harness created in Task 1 — cite its exact invocation and measured runtime)
  </read_first>
  <action>
Create `.planning/phases/05-design-artifact-ingestion/05-VALIDATION.md` mirroring the structure of
`01-VALIDATION.md`.

Frontmatter: `phase: 5`, `slug: design-artifact-ingestion`, `status: verified`, `nyquist_compliant: true`,
`wave_0_complete: true`, `created: 2026-08-25`.

`## Test Infrastructure` table with rows Framework, Config file, Quick run command, Full suite command,
Estimated runtime. Framework is Shell (bash) plus Node — the deliverables are a Node CLI script and a Claude
skill file, so there is no JS test runner. Quick run and full suite are both
`bash _scripts/verify-phase-05.sh`. Runtime is the value you measured in Task 1, not an estimate.

`## Sampling Rate` — after every task commit, after every plan wave, and before `/gsd:verify-work`, run
`bash _scripts/verify-phase-05.sh`; state the measured max feedback latency. No watch-mode flags.

`## Per-Task Verification Map` table with the same columns as 01-VALIDATION.md: Task ID, Plan, Wave,
Requirement, Threat Ref, Secure Behavior, Test Type, Automated Command, File Exists, Status. Populate one row
per task actually present in the three Phase 5 PLAN files — read them, do not invent task IDs. Requirement
IDs come from each plan's `requirements` frontmatter (the INGEST-* set). Automated Command is
`bash _scripts/verify-phase-05.sh <section>` for structurally verifiable tasks. Status is `✅ green` only for
rows whose command you actually ran and saw pass.

`## Wave 0 Requirements` — state that `_scripts/verify-phase-05.sh` was created during this Nyquist audit
(Phase 8, DEXP-06), consolidating the verify blocks from the three Phase 5 PLAN files into one runnable
artifact, and give its real check count.

`## Manual-Only Verifications` table with the columns Behavior, Requirement, Why Manual, Test Instructions.
Include at minimum:
  - the live artifact ingest round trip, marked resolved by the Task 2 run, with the exact commands, the
    observed `Hero.astro` transformations (BASE_URL alias, `${b}/images/hero.jpg` rewrite, `.hero`-scoped CSS
    only), and the cleanup confirmation
  - the collision-gate operator confirmation from `/wm-ingest` Step 4, which requires a human to type `y` and
    therefore cannot be automated
  - `--mode full` end-to-end ingest, which is manual-only because it overwrites `src/pages/index.astro` and
    cannot be exercised against a live site without destroying it

`## Validation Sign-Off` checklist mirroring 01-VALIDATION.md: every task has an automated verify or
manual-only documentation, sampling continuity holds (no 3 consecutive tasks without an automated verify),
Wave 0 harness created with its check count, no watch-mode flags, feedback latency under 5s,
`nyquist_compliant: true` set. Append an `## Validation Audit 2026-08-25` table with Gaps found / Resolved /
Escalated to manual-only counts.

Every claim in this document must trace to a command you ran in Task 1 or Task 2. If a Phase 5 task has no
automated command and no legitimate manual-only justification, record it as a gap rather than marking it
green.
  </action>
  <verify>
    <automated>bash _scripts/verify-phase-08.sh dexp 2&gt;&amp;1 | grep -E '05-VALIDATION|verify-phase-05'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-design-artifact-ingestion/05-VALIDATION.md` exists
    - its frontmatter contains `phase: 5`, `slug: design-artifact-ingestion`, `status: verified`, `nyquist_compliant: true`, `wave_0_complete: true`, `created: 2026-08-25`
    - the file contains all six headings: `## Test Infrastructure`, `## Sampling Rate`, `## Per-Task Verification Map`, `## Wave 0 Requirements`, `## Manual-Only Verifications`, `## Validation Sign-Off`
    - the file contains `verify-phase-05.sh` at least twice
    - the file contains `mode section` and `Hero.astro` (the live-test evidence)
    - the Per-Task Verification Map has at least one row per Phase 5 plan (01, 02, 03)
    - `python3 -c "import yaml;yaml.safe_load(open('.planning/phases/05-design-artifact-ingestion/05-VALIDATION.md').read().split('---')[1])"` exits 0
    - the two `05-VALIDATION.md` / `verify-phase-05.sh` assertions in `bash _scripts/verify-phase-08.sh dexp` report PASS
  </acceptance_criteria>
  <done>Phase 5 has a VALIDATION.md on disk with valid frontmatter, all six standard sections, a per-task verification map traceable to the three Phase 5 plans, and manual-only entries backed by the Task 2 live run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifact HTML -> repository filesystem | `ingest-artifact.mjs` parses untrusted HTML and writes derived `.astro` and image files into a site tree |
| section name argument -> output file path | `--section <name>` is PascalCased into `sites/<slug>/src/components/<Name>.astro` |
| base64 data URI -> `public/images/<slug>/` | `decodeBase64` writes binary content decoded from the artifact |
| validation record -> future verifier decisions | `05-VALIDATION.md` is consumed as ground truth by later GSD phases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-18 | Tampering | live ingest overwriting real site source | mitigate | Task 2 uses `--mode section` only, which `process.exit(0)`s after `writeSectionMode` and never touches `src/pages/`. `--mode full`, `--commit`, and `--target-repo` are explicitly forbidden by the action, and `git status --porcelain sites/sfdy-alt-clean/src/pages` is asserted empty at every step. |
| T-08-19 | Tampering | `--section hero` clobbering existing chrome | mitigate | Target verified in advance: `sites/sfdy-alt-clean/src/components/` has no `Hero.astro`, so nothing is overwritten. Task 2 runs `--dry-run` first and asserts the file still does not exist before the real run. |
| T-08-20 | Tampering | ingest silently patching `astro.config.mjs` (INGEST-02 injection path) | mitigate | Target chosen because `sites/sfdy-alt-clean/astro.config.mjs` already contains the SITE_URL/SITE_BASE pattern, so the guard reports "confirmed" and writes nothing; Task 2 asserts `git status --porcelain` on that file is empty |
| T-08-21 | Denial of service | leftover test artifacts polluting the repo and future builds | mitigate | Task 2 Step 4 deletes `Hero.astro`, `rmdir`s the created image directory only when empty, and asserts `git status --porcelain sites/sfdy-alt-clean` is empty |
| T-08-22 | Information disclosure | writing secrets into `05-VALIDATION.md` | mitigate | The document records commands, exit codes, file paths, and grep counts only. The action forbids nothing else; explicitly, no `--commit` or `--target-repo` invocation occurs, so no PAT or repo token appears in any recorded output. |
| T-08-23 | Repudiation | marking Phase 5 tasks green without running their commands | mitigate | Task 3 requires every `✅ green` row to correspond to a command actually run in Task 1 or Task 2, and requires unverifiable tasks to be recorded as gaps rather than green |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages. `ingest-artifact.mjs` imports `hast-util-from-html`, `hast-util-to-html`, `adm-zip`, and `turndown`, all already installed and audited during Phase 5 and Phase 6; no new dependency enters the tree, so no package legitimacy checkpoint is required. |
</threat_model>

<verification>
```bash
bash _scripts/verify-phase-05.sh all
node _scripts/ingest-artifact.mjs sfdy-alt-clean --analyze | head -20
git status --porcelain sites/sfdy-alt-clean
test ! -f sites/sfdy-alt-clean/src/components/Hero.astro
test -f .planning/phases/05-design-artifact-ingestion/05-VALIDATION.md
bash _scripts/verify-phase-08.sh dexp
```
</verification>

<success_criteria>
- `bash _scripts/verify-phase-05.sh all` exits 0 in under 5 seconds
- A real `--mode section` ingest ran, its output was inspected, and its BASE_URL rewriting and CSS scoping were confirmed
- `git status --porcelain sites/sfdy-alt-clean` is empty after cleanup
- `.planning/phases/05-design-artifact-ingestion/05-VALIDATION.md` exists with valid frontmatter, `nyquist_compliant: true`, and all six standard sections
- The `05-VALIDATION.md` and `verify-phase-05.sh` assertions in `verify-phase-08.sh dexp` pass
</success_criteria>

<output>
Create `.planning/phases/08-cleanup-verification/08-05-SUMMARY.md` when done.
Record the verify-phase-05.sh check count and runtime, the analyze JSON section/collision counts, the observed `Hero.astro` transformations, and confirmation that cleanup left the tree clean.
</output>
</content>
