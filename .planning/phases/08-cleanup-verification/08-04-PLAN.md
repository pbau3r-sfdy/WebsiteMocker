---
phase: 08-cleanup-verification
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - .planning/phases/02-content-system/02-VERIFICATION.md
autonomous: true
requirements: [DEXP-04]

must_haves:
  truths:
    - "A reader of 02-VERIFICATION.md sees a status that matches the current codebase — no open CR-01/CR-02 card-link warning"
    - "The document records concrete code evidence that all three _core card components prepend BASE_URL"
    - "The document records that the Phase 2 build uncertainty has been resolved by actual passing builds of all three Phase 2 active sites"
    - "No stale 'Human Verification Required' item remains outstanding in the document"
  artifacts:
    - path: ".planning/phases/02-content-system/02-VERIFICATION.md"
      provides: "accurate Phase 2 verification record"
      contains: "status: verified"
  key_links:
    - from: ".planning/phases/02-content-system/02-VERIFICATION.md"
      to: "commit 05e614a"
      via: "cited fix provenance for CR-01/CR-02"
      pattern: "05e614a"
    - from: ".planning/phases/02-content-system/02-VERIFICATION.md"
      to: "_scripts/verify-phase-02.sh"
      via: "cited automated re-verification command"
      pattern: "verify-phase-02\\.sh"
---

<objective>
Bring `.planning/phases/02-content-system/02-VERIFICATION.md` in line with the current codebase: the CR-01/CR-02 card-link defect it reports as unfixed was closed in commit `05e614a`, and the build uncertainty it reports can now be resolved by running the build.

Purpose: DEXP-04. A verification record that overstates open defects is worse than no record — it makes every future reader re-litigate a closed issue.
Output: a corrected `02-VERIFICATION.md` with code-level evidence, cited commit provenance, and zero outstanding human-verification items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cleanup-verification/08-CONTEXT.md
@.planning/phases/08-cleanup-verification/08-PATTERNS.md
@AGENTS.md

<interfaces>
<!-- Evidence the executor must confirm first-hand, then cite. Do not take these on trust. -->

Commit provenance: `05e614a` — "fix(02): prepend BASE_URL in AnnouncementCard, BlogCard, JobCard hrefs",
dated 2026-08-20, touching exactly three files (AnnouncementCard.astro +3/-1, BlogCard.astro +5/-2,
JobCard.astro +3/-1).

Current card state to confirm by reading the files:
  _core/src/components/AnnouncementCard.astro — `const b = import.meta.env.BASE_URL.replace(/\/$/, '')`, href `${b}/announcements/${id}`
  _core/src/components/BlogCard.astro         — same `b` alias, href `${b}/blog/${id}`, img `src={`${b}${image}`}`
  _core/src/components/JobCard.astro          — same `b` alias, href `${b}/jobs/${id}`

Automated re-verification harness that already exists: `_scripts/verify-phase-02.sh`.

Sections of `02-VERIFICATION.md` that carry stale claims (line numbers as of the current file):
  1-14    YAML frontmatter: `status: human_needed`, `score: 9/10`, two `human_verification` entries
  20      summary line: "human_needed — ... two items need human confirmation"
  40      truth table row 10: "? UNCERTAIN" for the all-sites-build truth
  130     behavioural spot-check row: "dist/ stale (pre-Plans 06-09) ... ? SKIP — needs human run"
  134-143 "Anti-Patterns Found": four rows, three of them describing the now-fixed root-relative hrefs
  147-167 "Human Verification Required": two subsections, both now resolvable
  170-180 "Gaps Summary": two outstanding items, plus a crestworks scope note

The crestworks scope note at the end of the Gaps Summary is NOT stale in the same way — crestworks content
routes are being added in Phase 8 plans 08-02 and 08-03. It must be updated to point at Phase 8, not deleted.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gather first-hand evidence and correct the evidence-bearing sections</name>
  <files>.planning/phases/02-content-system/02-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/02-content-system/02-VERIFICATION.md (the document being corrected — read it fully before editing)
    - _core/src/components/AnnouncementCard.astro (confirm the `b` alias and the `${b}/announcements/${id}` href)
    - _core/src/components/BlogCard.astro (confirm the `b` alias, `${b}/blog/${id}` href, and `${b}${image}` img src)
    - _core/src/components/JobCard.astro (confirm the `b` alias and the `${b}/jobs/${id}` href)
    - _scripts/verify-phase-02.sh (the automated harness to cite and run)
  </read_first>
  <action>
Per D-07, verify the code before touching the document. Run `git log --oneline -1 05e614a` and
`git show --stat 05e614a` to confirm the commit subject, date, and the three touched files. Read all three
card components and record, for each, the exact line number of the `const b = import.meta.env.BASE_URL`
declaration and of every `${b}`-prefixed href or src. Then run `bash _scripts/verify-phase-02.sh` and record
its pass/fail counts and exit code.

Then resolve the stale build uncertainty by building exactly the three sites truth 10 is about — the Phase 2
active sites — one at a time, each from its own directory: `cd sites/sfdy-alt-clean && npx astro build`, then
`cd sites/mogwai-systems && npx astro build`, then `cd sites/parrot-capital && npx astro build`. Record each
exit code and confirm `sites/<slug>/dist/jobs/index.html`, `sites/<slug>/dist/announcements/index.html`, and
`sites/<slug>/dist/blog/index.html` were emitted for all three. Do NOT run `npm run build` or
`node _scripts/build-all.js` here — plans 08-02 and 08-03 build `sites/crestworks` concurrently and a
monorepo-wide build would race them. The phase-wide regression build runs once in plan 08-06.

If any of that evidence contradicts the premise — for example a card still uses a root-relative href — STOP,
do not edit the document to claim a fix that is not there, and record the contradiction in the plan SUMMARY
as a blocker.

Then edit two sections of `02-VERIFICATION.md`:

Replace the "Anti-Patterns Found" table (currently four rows). The three CR-01/CR-02 rows become a single
row recording that the pattern was fixed in commit `05e614a` on 2026-08-20, with the confirmed current state
(`${b}` prefix present in all three card components at the line numbers you recorded) and severity `Resolved`.
Keep the fourth row (the WR-01 unused-`b`-alias dead code in nine site index files) only if it is still true —
re-check with `grep -n 'BASE_URL' sites/*/src/pages/{jobs,announcements,blog}/index.astro` and, if the alias
is now used by those pages, replace it with a `Resolved` row too. Preserve the closing sentence stating that
no TBD/FIXME/XXX markers were found.

Update the "Behavioral Spot-Checks" table row for builds: replace the `? SKIP — needs human run` result with
the actual per-site `npx astro build` outcomes for sfdy-alt-clean, mogwai-systems, and parrot-capital, citing
the emitted `sites/<slug>/dist/{jobs,announcements,blog}/index.html` paths as evidence, and mark it `✓ PASS`. Add one new row recording the `bash _scripts/verify-phase-02.sh` run with its real pass
count and `✓ PASS`.

Do not touch the Requirement Coverage, Required Artifacts, Key Link Verification, or Data-Flow Trace tables —
those were already accurate.
  </action>
  <verify>
    <automated>bash _scripts/verify-phase-02.sh &amp;&amp; for s in sfdy-alt-clean mogwai-systems parrot-capital; do (cd "sites/$s" &amp;&amp; npx astro build &gt;/dev/null) || exit 1; done &amp;&amp; grep -c '05e614a' .planning/phases/02-content-system/02-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash _scripts/verify-phase-02.sh` exits 0
    - `cd sites/sfdy-alt-clean && npx astro build` exits 0, and `sites/sfdy-alt-clean/dist/{jobs,announcements,blog}/index.html` all exist
    - `cd sites/mogwai-systems && npx astro build` exits 0, and `sites/mogwai-systems/dist/{jobs,announcements,blog}/index.html` all exist
    - `cd sites/parrot-capital && npx astro build` exits 0, and `sites/parrot-capital/dist/{jobs,announcements,blog}/index.html` all exist
    - `.planning/phases/02-content-system/02-VERIFICATION.md` contains `05e614a`
    - `.planning/phases/02-content-system/02-VERIFICATION.md` contains `verify-phase-02.sh`
    - `grep -c 'needs human run' .planning/phases/02-content-system/02-VERIFICATION.md` returns 0
    - `grep -c 'dist/ stale' .planning/phases/02-content-system/02-VERIFICATION.md` returns 0
    - `grep -cE 'root-relative href, no BASE_URL' .planning/phases/02-content-system/02-VERIFICATION.md` returns 0
    - the Anti-Patterns Found table still ends with the sentence about no TBD, FIXME, or XXX markers
    - `grep -c 'No TBD, FIXME, or XXX' .planning/phases/02-content-system/02-VERIFICATION.md` returns 1
  </acceptance_criteria>
  <done>The Anti-Patterns and Behavioral Spot-Checks sections state the real current code state, cite commit `05e614a` and the two verification commands with their actual results, and contain no claim that a card link is broken.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the frontmatter, truth table, human-verification, and gaps sections</name>
  <files>.planning/phases/02-content-system/02-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/02-content-system/02-VERIFICATION.md (as edited by Task 1)
    - .planning/phases/01-production-deploy-pipeline/01-VALIDATION.md (frontmatter conventions used elsewhere in .planning/phases)
    - .planning/ROADMAP.md (Phase 8 entry — the crestworks scope note must point here)
    - .planning/REQUIREMENTS.md (HSK-01 wording, for the crestworks scope note)
  </read_first>
  <action>
Update the YAML frontmatter of `.planning/phases/02-content-system/02-VERIFICATION.md`:
set `status: verified`, set `score: 10/10 must-haves verified`, remove both `human_verification` entries
entirely (the key and its list), keep `phase: 02-content-system` and `overrides_applied: 0`, and add
`re_verified: 2026-08-25` plus `re_verified_by: "Phase 8 DEXP-04"` so the correction is attributable.
Keep the existing `verified:` timestamp as the original verification date — do not overwrite history.

Update the summary line under the title from `human_needed — code implementation complete; two items need
human confirmation before proceeding` to a verified statement naming both resolutions: CR-01/CR-02 fixed in
`05e614a`, builds confirmed green. Change the `**Re-verification:**` line from `No — initial verification`
to `Yes — corrected 2026-08-25 by Phase 8 (DEXP-04)`.

Update truth-table row 10 (the all-sites-build truth): change the Status cell from `? UNCERTAIN` to
`✓ VERIFIED` and replace the Evidence cell with the concrete per-site `npx astro build` exit codes and the
emitted `sites/<slug>/dist/{jobs,announcements,blog}/index.html` paths recorded in Task 1. Update the `**Score:**` line below the table from
`9/10 truths verified` to `10/10 truths verified`.

Replace the entire "Human Verification Required" section (both numbered subsections) with a short
"Human Verification — Resolved" section containing a two-row table: one row for the build verification
(resolved by the Task 1 per-site `npx astro build` runs, with their evidence) and one row for CR-01/CR-02 (resolved by
commit `05e614a`, with the confirmed card line numbers). Keep the section heading level consistent with its
siblings.

Rewrite the "Gaps Summary" section to state zero remaining gaps, listing the two former items as closed with
their resolutions. Keep the trailing crestworks scope note but update it: crestworks is an active stage-6
site, it was out of scope for Phase 2 per `02-RESEARCH.md`, and its content routes are being delivered by
Phase 8 requirement HSK-01 (plans 08-02 and 08-03) — so the scope boundary is now reconciled rather than
open. Do not claim the crestworks work is complete unless it is; phrase it as the reconciliation path.

Leave the closing `_Verified: 2026-08-20_` / `_Verifier: Claude (gsd-verifier)_` footer intact and append a
single line recording the 2026-08-25 Phase 8 correction.
  </action>
  <verify>
    <automated>bash _scripts/verify-phase-08.sh dexp 2&gt;&amp;1 | grep -E 'status: verified|02-VERIFICATION'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-content-system/02-VERIFICATION.md` frontmatter contains `status: verified`
    - `grep -c 'human_needed' .planning/phases/02-content-system/02-VERIFICATION.md` returns 0
    - `grep -c 'human_verification:' .planning/phases/02-content-system/02-VERIFICATION.md` returns 0
    - `grep -c '? UNCERTAIN' .planning/phases/02-content-system/02-VERIFICATION.md` returns 0
    - `.planning/phases/02-content-system/02-VERIFICATION.md` contains `10/10`
    - `.planning/phases/02-content-system/02-VERIFICATION.md` contains `re_verified: 2026-08-25`
    - `.planning/phases/02-content-system/02-VERIFICATION.md` contains `HSK-01`
    - `grep -c 'Human Verification Required' .planning/phases/02-content-system/02-VERIFICATION.md` returns 0
    - `python3 -c "import sys,re;t=open('.planning/phases/02-content-system/02-VERIFICATION.md').read();b=t.split('---')[1];import yaml;yaml.safe_load(b)"` exits 0 (frontmatter is still valid YAML)
    - the two `02-VERIFICATION.md` assertions in `bash _scripts/verify-phase-08.sh dexp` report PASS
  </acceptance_criteria>
  <done>`02-VERIFICATION.md` frontmatter, summary, truth table, human-verification, and gaps sections all describe the real current state; the document has valid YAML frontmatter, `status: verified`, a 10/10 score, and zero outstanding human-verification items.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification record -> future planner/verifier decisions | `02-VERIFICATION.md` is consumed as ground truth by later GSD phases; a false claim in it propagates into planning decisions |
| git history -> cited evidence | commit SHA `05e614a` is the provenance anchor for the CR-01/CR-02 closure claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | Repudiation | rewriting a verification record | mitigate | Task 2 preserves the original `verified:` timestamp and footer and adds `re_verified` / `re_verified_by` fields plus an appended correction line, so the amendment is attributable rather than silent |
| T-08-15 | Tampering | claiming a fix that is not present | mitigate | Task 1 requires first-hand reads of all three card components plus `git show --stat 05e614a`, and explicitly instructs the executor to STOP and record a blocker rather than write an unsupported claim |
| T-08-16 | Tampering | marking truth 10 verified without a build | mitigate | The builds must actually run; the acceptance criteria require exit 0 from `npx astro build` in each of the three Phase 2 site directories and the emitted `sites/<slug>/dist/{jobs,announcements,blog}/index.html` paths as cited evidence |
| T-08-17 | Information disclosure | verification doc contents | accept | The document records file paths, commit SHAs, and grep counts only. No tokens, PAT values, domains under NDA, or PII are added. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages. `npx astro build` resolves only what the existing workspace lockfile pins; no new dependency enters the tree, so no package legitimacy checkpoint is required. |
</threat_model>

<verification>
```bash
git show --stat 05e614a
grep -n 'BASE_URL' _core/src/components/AnnouncementCard.astro _core/src/components/BlogCard.astro _core/src/components/JobCard.astro
bash _scripts/verify-phase-02.sh
for s in sfdy-alt-clean mogwai-systems parrot-capital; do (cd "sites/$s" && npx astro build) || echo "FAIL $s"; done
grep -c 'human_needed' .planning/phases/02-content-system/02-VERIFICATION.md
grep -c '05e614a' .planning/phases/02-content-system/02-VERIFICATION.md
```
</verification>

<success_criteria>
- `02-VERIFICATION.md` has `status: verified`, a 10/10 score, and valid YAML frontmatter
- Zero occurrences of `human_needed`, `human_verification:`, `? UNCERTAIN`, `needs human run`, or `root-relative href, no BASE_URL`
- Commit `05e614a` and `_scripts/verify-phase-02.sh` are both cited as evidence
- `bash _scripts/verify-phase-02.sh` exits 0, and `npx astro build` exits 0 in each of `sites/sfdy-alt-clean`, `sites/mogwai-systems`, `sites/parrot-capital`
- The crestworks scope note points at Phase 8 / HSK-01 as the reconciliation path
</success_criteria>

<output>
Create `.planning/phases/08-cleanup-verification/08-04-SUMMARY.md` when done.
Record the confirmed card-component line numbers, the `verify-phase-02.sh` pass count, and the three per-site build exit codes.
</output>
</content>
