---
phase: 08-cleanup-verification
plan: 06
type: execute
wave: 4
depends_on: ["08-03", "08-05"]
files_modified:
  - .planning/phases/04-collaboration-infrastructure/04-VALIDATION.md
autonomous: true
requirements: [DEXP-05]

must_haves:
  truths:
    - "A contributor pushing a Markdown file to a production repo's content/ folder provably lands that file in WebsiteMocker for operator review"
    - "That contributor push provably does NOT change the live site"
    - "Deleting the file from the production repo provably does NOT remove it from WebsiteMocker (additive-only policy)"
    - "Phase 4 has a VALIDATION.md on disk backed by that live round-trip, not by inference"
    - "The live test leaves no residue in either repository"
  artifacts:
    - path: ".planning/phases/04-collaboration-infrastructure/04-VALIDATION.md"
      provides: "Nyquist validation record for Phase 4, including the live-fire contributor round trip"
      contains: "nyquist_compliant: true"
      min_lines: 50
  key_links:
    - from: ".planning/phases/04-collaboration-infrastructure/04-VALIDATION.md"
      to: "_scripts/verify-phase-04.sh"
      via: "cited quick-run and full-suite command"
      pattern: "verify-phase-04\\.sh"
    - from: ".planning/phases/04-collaboration-infrastructure/04-VALIDATION.md"
      to: "live content-ci -> content-sync round trip evidence"
      via: "recorded workflow run IDs, synced path, and cleanup confirmation"
      pattern: "content-sync"
    - from: "pbau3r-sfdy/mogwai-systems content-ci.yml"
      to: "pbau3r-sfdy/WebsiteMocker content-sync.yml"
      via: "repository_dispatch event-type content-updated"
      pattern: "content-updated"
---

<objective>
Close the last open Phase 4 uncertainty by running the contributor round trip for real against a live production repo, then record the result in `04-VALIDATION.md`. Finish the phase with a full-monorepo regression build and a green `verify-phase-08.sh all`.

Purpose: DEXP-05. `04-VERIFICATION.md` scored 3/4 with truth 3 marked UNCERTAIN because the live-fire test was never performed. This plan performs it.
Output: a recorded live round trip, `.planning/phases/04-collaboration-infrastructure/04-VALIDATION.md`, and a green phase-wide verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cleanup-verification/08-CONTEXT.md
@.planning/phases/08-cleanup-verification/08-PATTERNS.md
@AGENTS.md

<interfaces>
<!-- Contracts the executor needs. Do not re-derive these by exploring the codebase or the GitHub UI. -->

Chosen live-test target: `pbau3r-sfdy/mogwai-systems` (Claude's discretion per 08-CONTEXT; also the target
recommended in `04-VERIFICATION.md`'s human_verification entry).

Pre-flight state already confirmed and safe to rely on:
  - `gh auth status` reports account pbau3r-sfdy with scopes admin:org, delete_repo, gist, repo, workflow
  - `pbau3r-sfdy/mogwai-systems` default branch is `main` and holds
    `.github/workflows/content-ci.yml` with `client-payload: '{"slug": "mogwai-systems"}'`
  - `pbau3r-sfdy/mogwai-systems` has an Actions secret named `WM_DISPATCH_PAT`
  - `pbau3r-sfdy/mogwai-systems` has `content/news`, `content/jobs`, `content/announcements`, `content/blog`
  - `pbau3r-sfdy/WebsiteMocker` has `.github/workflows/content-sync.yml` on `main` and an Actions secret
    named `WM_PUBLISH_PAT`
  - remote `origin/main` for WebsiteMocker already contains `sites/mogwai-systems/wiring.json` with
    `"prod_repo": "pbau3r-sfdy/mogwai-systems"`, which is what `content-sync.yml` reads

Trigger contract of `content-ci.yml` in the production repo:
  on: push to branch main, paths filter `content/**/*.md`
  action: peter-evans/repository-dispatch@v4 -> repository pbau3r-sfdy/WebsiteMocker,
          event-type `content-updated`, client-payload `{"slug": "mogwai-systems"}`
  There is no workflow_dispatch — the only way to fire it is an actual content push.

Behaviour contract of `content-sync.yml` in WebsiteMocker:
  on: repository_dispatch types [content-updated] (also workflow_dispatch with a slug input, debugging only)
  resolves the slug (regex-gated to ^[a-z0-9-]+$), reads sites/<slug>/wiring.json for prod_repo,
  `git clone --depth 1 --branch main` the prod repo, then for every `find content -type f -name '*.md'`
  copies to `sites/<slug>/src/content/<relpath>`, commits `chore(<slug>): sync content from production repo`,
  and pushes to WebsiteMocker `main`.
  It is additive by design (decision D-A4): a deletion in the production repo is NOT propagated.
  It NEVER builds and NEVER publishes — the live site changes only when the operator runs `/wm-publish`.

CRITICAL local-repo constraint: the local WebsiteMocker clone is 22 commits AHEAD of `origin/main` and 0
behind. `content-sync.yml` commits to the REMOTE `main`. Therefore this plan verifies and cleans up entirely
through `gh api` and `git fetch` against the remote. Do NOT run `git pull`, `git merge`, `git rebase`, or
`git push` at any point — a local integration here would entangle 22 unpushed commits with a throwaway test.

Format model for the output document: `.planning/phases/01-production-deploy-pipeline/01-VALIDATION.md`
(frontmatter keys phase/slug/status/nyquist_compliant/wave_0_complete/created; sections Test Infrastructure,
Sampling Rate, Per-Task Verification Map, Wave 0 Requirements, Manual-Only Verifications, Validation Sign-Off).

Existing Phase 4 harness to cite: `_scripts/verify-phase-04.sh` (64 checks, section filter 01|02|03|04|all).

Test file identity used throughout this plan:
  path in prod repo:      content/news/2026-08-25-e2e-sync-test.md
  expected synced path:   sites/mogwai-systems/src/content/news/2026-08-25-e2e-sync-test.md
  unique marker string:   E2E-SYNC-TEST-DEXP05
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-fire contributor round trip</name>
  <files>.planning/phases/04-collaboration-infrastructure/04-VALIDATION.md</files>
  <read_first>
    - .github/workflows/content-sync.yml (the receiving workflow: slug regex gate, wiring.json read, additive find/cp loop, commit message, push target)
    - _templates/.github/workflows/content-ci.yml (the dispatching workflow: paths filter, event-type, client-payload shape)
    - .planning/phases/04-collaboration-infrastructure/04-VERIFICATION.md (truth 3 is the UNCERTAIN item this task resolves; its human_verification block states the exact expected behaviour)
    - _core/src/content.config.ts (newsSchema — the test file's frontmatter must satisfy title, date, summary or a later build will fail)
    - sites/mogwai-systems/wiring.json (prod_repo value that content-sync.yml resolves)
  </read_first>
  <action>
Run the contributor round trip against `pbau3r-sfdy/mogwai-systems` using `gh` only. Record every command,
its exit code, and every workflow run ID and conclusion — Task 3 turns these into the validation record.

Step 1 — capture the baseline. Record `gh api repos/pbau3r-sfdy/WebsiteMocker/commits/main --jq '.sha'` as
the pre-test WebsiteMocker remote head. Record
`gh api repos/pbau3r-sfdy/WebsiteMocker/contents/sites/mogwai-systems/src/content/news --jq '[.[].name]'`
as the pre-test synced inventory (expect `.gitkeep` only). Confirm
`gh api repos/pbau3r-sfdy/mogwai-systems/contents/content/news --jq '[.[].name]'` shows no
`2026-08-25-e2e-sync-test.md` — if it is already there from an earlier attempt, delete it first and let the
resulting sync settle before starting.

Step 2 — push the test content file. Compose a Markdown file whose frontmatter satisfies newsSchema:
`title: "E2E sync test — DEXP-05"`, `date: "2026-08-25"`,
`summary: "Temporary verification artifact for Phase 8 DEXP-05. Safe to delete."`, and a body containing the
unique marker `E2E-SYNC-TEST-DEXP05` plus a one-line note that the file is a throwaway test. Base64-encode it
and create it on `main` with a single call:
`gh api --method PUT repos/pbau3r-sfdy/mogwai-systems/contents/content/news/2026-08-25-e2e-sync-test.md`
passing `message`, `content` (base64), and `branch=main`. Record the returned commit SHA.

Step 3 — observe the dispatching workflow. Poll `gh run list -R pbau3r-sfdy/mogwai-systems -w "Content CI"
--limit 3 --json databaseId,status,conclusion,headSha` until the run whose headSha matches the Step 2 commit
reaches `completed`. Record its run ID and conclusion; it must be `success`. If it is `failure`, fetch
`gh run view <id> -R pbau3r-sfdy/mogwai-systems --log-failed`, record the reason, and STOP — do not fabricate
a passing result. A `WM_DISPATCH_PAT` permission error here is a real finding to report, not something to
work around.

Step 4 — observe the receiving workflow. Poll
`gh run list -R pbau3r-sfdy/WebsiteMocker -w "Sync Content from Production Repo" --limit 3 --json
databaseId,status,conclusion,event` until a run with `event: repository_dispatch` started after Step 2
reaches `completed`. Record its run ID and conclusion; it must be `success`. Record the run's step summary
with `gh run view <id> -R pbau3r-sfdy/WebsiteMocker` and note the `synced:` line for the test file.

Step 5 — verify the sync landed on the remote. Confirm
`gh api repos/pbau3r-sfdy/WebsiteMocker/contents/sites/mogwai-systems/src/content/news/2026-08-25-e2e-sync-test.md`
returns 200 and that its decoded content contains the marker `E2E-SYNC-TEST-DEXP05`. Confirm
`gh api repos/pbau3r-sfdy/WebsiteMocker/commits/main --jq '.commit.message'` is
`chore(mogwai-systems): sync content from production repo` and that its SHA differs from the Step 1 baseline.

Step 6 — verify the live site did NOT change (decision D-A6 / D-A7). Run
`curl -sSL https://mogwai-systems.com/news/` and `curl -sSL https://mogwai-systems.com/` and confirm neither
response body contains `E2E-SYNC-TEST-DEXP05` or `E2E sync test`. Record the HTTP status codes. Also confirm
no `Publish Site` / `publish.yml` run was triggered:
`gh run list -R pbau3r-sfdy/WebsiteMocker -w publish.yml --limit 3 --json databaseId,createdAt` must show no
run created after the Step 2 commit timestamp.

Do NOT run `git pull`, `git merge`, `git rebase`, or `git push` in this task. Do NOT run `/wm-publish` or
dispatch `publish.yml` for any site. Do NOT modify any file in the local working tree in this task other than
appending evidence notes you intend to place into `04-VALIDATION.md` in Task 3.
  </action>
  <verify>
    <automated>gh api repos/pbau3r-sfdy/WebsiteMocker/contents/sites/mogwai-systems/src/content/news/2026-08-25-e2e-sync-test.md --jq '.content' | base64 -d | grep -c 'E2E-SYNC-TEST-DEXP05'</automated>
  </verify>
  <acceptance_criteria>
    - `gh api --method PUT repos/pbau3r-sfdy/mogwai-systems/contents/content/news/2026-08-25-e2e-sync-test.md` returned a commit SHA
    - the `Content CI` run in `pbau3r-sfdy/mogwai-systems` matching that head SHA has `conclusion: success`
    - a `Sync Content from Production Repo` run in `pbau3r-sfdy/WebsiteMocker` with `event: repository_dispatch` has `conclusion: success`
    - `gh api repos/pbau3r-sfdy/WebsiteMocker/contents/sites/mogwai-systems/src/content/news/2026-08-25-e2e-sync-test.md` returns 200 and the decoded body contains `E2E-SYNC-TEST-DEXP05`
    - `gh api repos/pbau3r-sfdy/WebsiteMocker/commits/main --jq '.commit.message'` equals `chore(mogwai-systems): sync content from production repo`
    - `curl -sSL https://mogwai-systems.com/news/ | grep -c 'E2E-SYNC-TEST-DEXP05'` returns 0
    - `curl -sSL https://mogwai-systems.com/ | grep -c 'E2E-SYNC-TEST-DEXP05'` returns 0
    - `gh run list -R pbau3r-sfdy/WebsiteMocker -w publish.yml --limit 3` shows no run created after the Step 2 commit
    - `git status --porcelain` shows no new local commits and no local file changes from this task
  </acceptance_criteria>
  <done>A real contributor push to `pbau3r-sfdy/mogwai-systems` fired `content-ci.yml`, which dispatched `content-sync.yml`, which committed the file into `sites/mogwai-systems/src/content/news/` on WebsiteMocker's remote main — and the live mogwai-systems.com site is provably unchanged with no publish run triggered.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the additive-only policy, then clean up both repositories</name>
  <files>.planning/phases/04-collaboration-infrastructure/04-VALIDATION.md</files>
  <read_first>
    - .github/workflows/content-sync.yml (the additive `find … -print0` / `cp` loop with no deletion pass — this is what makes deletions non-propagating, per decision D-A4)
    - _templates/CONTRIBUTING.md (the additive-only policy as documented to contributors — the wording the test must substantiate)
    - .planning/phases/04-collaboration-infrastructure/04-VERIFICATION.md (its second human_verification entry defines the exact additive-only test)
  </read_first>
  <action>
Step 1 — delete the test file from the production repo. Fetch its blob SHA with
`gh api repos/pbau3r-sfdy/mogwai-systems/contents/content/news/2026-08-25-e2e-sync-test.md --jq '.sha'`, then
`gh api --method DELETE` the same path passing `message`, that `sha`, and `branch=main`. Record the returned
commit SHA. Note that this deletion is itself a push touching `content/**/*.md`, so `content-ci.yml` will
fire again — that is expected and is precisely what makes this a valid additive-only test.

Step 2 — observe the second round trip. Poll for the new `Content CI` run in
`pbau3r-sfdy/mogwai-systems` and the new `Sync Content from Production Repo` run in
`pbau3r-sfdy/WebsiteMocker`, recording both run IDs and conclusions. The sync run may conclude `success` with
a `No content changes` commit step outcome — record whichever it is verbatim rather than assuming.

Step 3 — assert the additive-only guarantee. Confirm
`gh api repos/pbau3r-sfdy/WebsiteMocker/contents/sites/mogwai-systems/src/content/news/2026-08-25-e2e-sync-test.md`
still returns 200 and still contains `E2E-SYNC-TEST-DEXP05`, while
`gh api repos/pbau3r-sfdy/mogwai-systems/contents/content/news/2026-08-25-e2e-sync-test.md` now returns 404.
That asymmetry is the evidence for decision D-A4.

Step 4 — clean up WebsiteMocker's remote. Fetch the blob SHA of
`sites/mogwai-systems/src/content/news/2026-08-25-e2e-sync-test.md` on WebsiteMocker main and
`gh api --method DELETE` it with a message naming Phase 8 DEXP-05 test cleanup and `branch=main`. Then
confirm the path returns 404 and that
`gh api repos/pbau3r-sfdy/WebsiteMocker/contents/sites/mogwai-systems/src/content/news --jq '[.[].name]'`
matches the Task 1 Step 1 baseline inventory exactly.

Step 5 — record the local/remote divergence without touching it. Run `git fetch origin` (read-only) and
`git rev-list --left-right --count origin/main...HEAD`, and record both numbers. Explain in the SUMMARY that
the remote gained three throwaway commits (sync, and the two deletions) whose net file effect is zero, so the
eventual push of the local branch will merge cleanly. Do NOT run `git pull`, `git merge`, `git rebase`, or
`git push`.

Also confirm no publish was triggered by either round trip:
`gh run list -R pbau3r-sfdy/WebsiteMocker -w publish.yml --limit 5` must show no run created during this plan.
  </action>
  <verify>
    <automated>gh api repos/pbau3r-sfdy/mogwai-systems/contents/content/news/2026-08-25-e2e-sync-test.md 2&gt;&amp;1 | grep -c 'Not Found' &amp;&amp; gh api repos/pbau3r-sfdy/WebsiteMocker/contents/sites/mogwai-systems/src/content/news/2026-08-25-e2e-sync-test.md 2&gt;&amp;1 | grep -c 'Not Found'</automated>
  </verify>
  <acceptance_criteria>
    - the `gh api --method DELETE` against the production repo returned a commit SHA
    - a second `Content CI` run in `pbau3r-sfdy/mogwai-systems` completed, with its conclusion recorded
    - a second `Sync Content from Production Repo` run in `pbau3r-sfdy/WebsiteMocker` completed, with its conclusion recorded
    - immediately after Step 3, the WebsiteMocker path returned 200 while the production-repo path returned 404 (additive-only proven)
    - after Step 4, `gh api repos/pbau3r-sfdy/WebsiteMocker/contents/sites/mogwai-systems/src/content/news/2026-08-25-e2e-sync-test.md` returns `Not Found`
    - after Step 4, `gh api repos/pbau3r-sfdy/WebsiteMocker/contents/sites/mogwai-systems/src/content/news --jq '[.[].name]'` equals the Task 1 baseline inventory
    - `gh run list -R pbau3r-sfdy/WebsiteMocker -w publish.yml --limit 5` shows no run created during this plan
    - `git status --porcelain` shows no unintended local file changes, and `git rev-list --left-right --count origin/main...HEAD` was recorded
    - no `git pull`, `git merge`, `git rebase`, or `git push` command appears in the task command log
  </acceptance_criteria>
  <done>The additive-only policy is proven by a live deletion that removed the file from the production repo while leaving it in WebsiteMocker, both repositories are back to their pre-test file inventories, no publish ran, and the local working tree and branch were never integrated with the remote.</done>
</task>

<task type="auto">
  <name>Task 3: Write 04-VALIDATION.md and run the phase-wide regression gate</name>
  <files>.planning/phases/04-collaboration-infrastructure/04-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01-production-deploy-pipeline/01-VALIDATION.md (the exact frontmatter keys, section headings, table columns, and status legend to mirror)
    - .planning/phases/04-collaboration-infrastructure/04-01-PLAN.md through 04-05-PLAN.md (task inventory, requirement IDs, threat refs, and verify blocks for the per-task map)
    - .planning/phases/04-collaboration-infrastructure/04-01-SUMMARY.md through 04-05-SUMMARY.md (what each plan actually delivered)
    - .planning/phases/04-collaboration-infrastructure/04-VERIFICATION.md (the 3/4 score and the two human_verification entries this plan resolves)
    - _scripts/verify-phase-04.sh (64 checks, section filter 01|02|03|04|all — cite its exact invocation and measured runtime)
  </read_first>
  <action>
Create `.planning/phases/04-collaboration-infrastructure/04-VALIDATION.md` mirroring the structure of
`01-VALIDATION.md`.

Frontmatter: `phase: 4`, `slug: collaboration-infrastructure`, `status: verified`,
`nyquist_compliant: true`, `wave_0_complete: true`, `created: 2026-08-25`.

`## Test Infrastructure` table with rows Framework, Config file, Quick run command, Full suite command,
Estimated runtime. Framework is Shell (bash) plus the `gh` CLI — the deliverables are GitHub Actions
workflows, YAML issue templates, a Markdown contributor guide, and a Node bootstrap script, so there is no JS
test runner. Quick run and full suite are both `bash _scripts/verify-phase-04.sh`. Runtime must be the value
you measure, not an estimate.

`## Sampling Rate` — after every task commit, after every plan wave, and before `/gsd:verify-work`, run
`bash _scripts/verify-phase-04.sh`; state the measured max feedback latency. No watch-mode flags.

`## Per-Task Verification Map` table with the same columns as 01-VALIDATION.md: Task ID, Plan, Wave,
Requirement, Threat Ref, Secure Behavior, Test Type, Automated Command, File Exists, Status. Populate one row
per task actually present in the five Phase 4 PLAN files — read them, do not invent task IDs. Requirement IDs
and threat refs come from each plan's frontmatter and `<threat_model>`. Automated Command is
`bash _scripts/verify-phase-04.sh <section>` for structurally verifiable tasks and the live `gh` commands
from Tasks 1 and 2 for the round-trip rows. Mark a row `✅ green` only if you actually ran its command and saw
it pass.

`## Wave 0 Requirements` — state that `_scripts/verify-phase-04.sh` already existed with 64 checks and
required no new scaffold, so `wave_0_complete: true` reflects pre-existing infrastructure rather than new work
in this plan.

`## Manual-Only Verifications` table with columns Behavior, Requirement, Why Manual, Test Instructions.
Include, each marked resolved with its evidence:
  - the live contributor round trip from Task 1 — record the production-repo commit SHA, both workflow run
    IDs and conclusions, the synced path, the `chore(mogwai-systems): sync content from production repo`
    commit message, and the two `curl` results proving the live site was unchanged
  - the additive-only policy from Task 2 — record the deletion commit SHA, the second pair of run IDs, and
    the 200-versus-404 asymmetry that proves D-A4
  - GitHub web UI file creation as a contributor experience, which remains genuinely manual because it needs
    a browser session as a non-owner account; note that Task 1 exercised the identical code path via the
    contents API and record that substitution explicitly rather than claiming browser coverage

`## Validation Sign-Off` checklist mirroring 01-VALIDATION.md, plus an `## Validation Audit 2026-08-25` table
with Gaps found / Resolved / Escalated to manual-only counts. Add a closing note that `04-VERIFICATION.md`
truth 3 (previously `? UNCERTAIN`) is now substantiated by the Task 1 live-fire evidence, citing the run IDs.

Then run the phase-wide regression gate, which runs here because plan 08-06 executes alone in the final wave:
`npm run build` from the repo root must exit 0, and `bash _scripts/verify-phase-08.sh all` must exit 0.
If `verify-phase-08.sh all` fails, fix the underlying gap — do not weaken the assertion.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; bash _scripts/verify-phase-08.sh all &amp;&amp; bash _scripts/verify-phase-04.sh &amp;&amp; bash _scripts/verify-phase-05.sh all &amp;&amp; bash _scripts/verify-phase-02.sh</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-collaboration-infrastructure/04-VALIDATION.md` exists
    - its frontmatter contains `phase: 4`, `slug: collaboration-infrastructure`, `status: verified`, `nyquist_compliant: true`, `wave_0_complete: true`, `created: 2026-08-25`
    - the file contains all six headings: `## Test Infrastructure`, `## Sampling Rate`, `## Per-Task Verification Map`, `## Wave 0 Requirements`, `## Manual-Only Verifications`, `## Validation Sign-Off`
    - the file contains `verify-phase-04.sh` at least twice
    - the file contains `content-sync`, `content-updated`, and `E2E-SYNC-TEST-DEXP05`
    - the Per-Task Verification Map has at least one row per Phase 4 plan (01 through 05)
    - `python3 -c "import yaml;yaml.safe_load(open('.planning/phases/04-collaboration-infrastructure/04-VALIDATION.md').read().split('---')[1])"` exits 0
    - `npm run build` exits 0
    - `bash _scripts/verify-phase-08.sh all` exits 0
    - `bash _scripts/verify-phase-04.sh` exits 0
    - `bash _scripts/verify-phase-05.sh all` exits 0
    - `bash _scripts/verify-phase-02.sh` exits 0
  </acceptance_criteria>
  <done>Phase 4 has a VALIDATION.md on disk whose live-fire and additive-only rows cite real workflow run IDs and commit SHAs, and the whole phase passes: `npm run build`, `verify-phase-08.sh all`, `verify-phase-04.sh`, `verify-phase-05.sh all`, and `verify-phase-02.sh` all exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor push -> production repo main | a write to `content/**/*.md` on a live production repo's default branch |
| production repo -> WebsiteMocker | `repository_dispatch` crossing repositories, carrying an attacker-controllable `client_payload.slug` |
| WebsiteMocker remote main -> live site | only `/wm-publish` may cross this boundary; `content-sync.yml` must never cross it |
| local clone (22 unpushed commits) -> remote main | the test mutates remote main only; entangling the local branch would put unrelated work at risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-24 | Tampering | writing a test file to a live production repo's main branch | mitigate | Single scoped path `content/news/2026-08-25-e2e-sync-test.md` with the unique marker `E2E-SYNC-TEST-DEXP05`; Task 2 Step 1 deletes it and Task 2 Step 4 asserts both repositories match their recorded pre-test inventories |
| T-08-25 | Elevation of privilege | accidental publish to mogwai-systems.com | mitigate | The plan forbids `/wm-publish` and any `publish.yml` dispatch; Task 1 Step 6 and Task 2 assert `gh run list -w publish.yml` shows no run created during the plan; two `curl` checks assert the marker never appears on the live site |
| T-08-26 | Tampering | `client_payload.slug` injection into the sync workflow's `run:` body | mitigate | Already mitigated in `content-sync.yml` (threat T-04-01): the slug is passed via the `DISPATCH_SLUG` env var, never interpolated, and gated by `^[a-z0-9-]+$`. This plan verifies the control holds by observing a real dispatch rather than re-implementing it. |
| T-08-27 | Tampering | non-Markdown or symlinked files entering the WebsiteMocker source tree | mitigate | Already mitigated in `content-sync.yml` (threat T-04-02) via `find -type f -name '*.md'`. Task 1 Step 5 asserts the synced inventory contains only the expected `.md` plus the pre-existing `.gitkeep`. |
| T-08-28 | Information disclosure | PAT values appearing in recorded workflow output | mitigate | Only `gh run view` summaries and `gh api` JSON are recorded. GitHub masks secret values in logs, and `WM_DISPATCH_PAT` / `WM_PUBLISH_PAT` are never printed or passed on the command line by this plan. Task 3 forbids pasting raw log bodies containing credential material into `04-VALIDATION.md`. |
| T-08-29 | Denial of service | entangling 22 unpushed local commits with throwaway remote test commits | mitigate | The plan bans `git pull`, `git merge`, `git rebase`, and `git push`; all verification and cleanup goes through `gh api`, and Task 2 Step 5 records the divergence read-only via `git fetch` plus `git rev-list --left-right --count` |
| T-08-30 | Repudiation | marking Phase 4 tasks green without running their commands | mitigate | Task 3 requires every `✅ green` row to cite a command actually run, requires real workflow run IDs and commit SHAs, and forbids claiming browser coverage for the contents-API substitution |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages. `npm run build` resolves only what the existing workspace lockfile pins. The third-party GitHub Actions in the round trip (`peter-evans/repository-dispatch@v4`, `actions/checkout@v7`) were audited during Phase 4 and are unchanged here; the floating-major pin on `repository-dispatch` is a known, documented acceptance recorded in `_templates/.github/workflows/content-ci.yml`. |
</threat_model>

<verification>
```bash
gh api repos/pbau3r-sfdy/mogwai-systems/contents/content/news --jq '[.[].name]'
gh api repos/pbau3r-sfdy/WebsiteMocker/contents/sites/mogwai-systems/src/content/news --jq '[.[].name]'
gh run list -R pbau3r-sfdy/WebsiteMocker -w publish.yml --limit 5
curl -sSL https://mogwai-systems.com/news/ | grep -c 'E2E-SYNC-TEST-DEXP05' || echo "clean"
test -f .planning/phases/04-collaboration-infrastructure/04-VALIDATION.md
npm run build
bash _scripts/verify-phase-08.sh all
bash _scripts/verify-phase-04.sh
bash _scripts/verify-phase-05.sh all
bash _scripts/verify-phase-02.sh
```
</verification>

<success_criteria>
- The live contributor round trip completed: `Content CI` success in the production repo, `Sync Content from Production Repo` success in WebsiteMocker, and the file present at `sites/mogwai-systems/src/content/news/` on remote main
- mogwai-systems.com never showed the test marker and no `publish.yml` run was created
- The additive-only policy is proven by a 200-versus-404 asymmetry after the production-repo deletion
- Both repositories match their recorded pre-test file inventories after cleanup
- No `git pull` / `merge` / `rebase` / `push` was executed; the 22 unpushed local commits are untouched
- `.planning/phases/04-collaboration-infrastructure/04-VALIDATION.md` exists with valid frontmatter, `nyquist_compliant: true`, all six sections, and live evidence citing real run IDs
- `npm run build`, `verify-phase-08.sh all`, `verify-phase-04.sh`, `verify-phase-05.sh all`, and `verify-phase-02.sh` all exit 0
</success_criteria>

<output>
Create `.planning/phases/08-cleanup-verification/08-06-SUMMARY.md` when done.
Record: both production-repo commit SHAs, all four workflow run IDs with conclusions, the synced path and sync commit message, the two `curl` results, the pre/post file inventories for both repos, the `git rev-list --left-right --count origin/main...HEAD` numbers, and the exit codes of the five verification commands.
</output>
</content>
