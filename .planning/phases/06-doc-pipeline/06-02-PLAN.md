---
phase: 06-doc-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - _scripts/ingest-artifact.mjs
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-04
  - DOCS-05
  - DOCS-06
must_haves:
  truths:
    - "node _scripts/ingest-artifact.mjs sfdy-alt-clean --mode docs --dry-run exits 1 with a message containing 'doc_tokens'"
    - "Given artifact.html and brand.doc_tokens set, the script produces _captures/<slug>/docs/index.html with overridden :root vars and exits 0"
    - "Given a .zip in _captures/<slug>/raw/ with one HTML file, the script auto-extracts and processes it"
    - "node _scripts/ingest-artifact.mjs sfdy-alt-clean --mode docs --commit --dry-run prints '[dry]' lines (no actual gh api call)"
    - "Passing --format md produces a docs/<name>.md GFM file alongside the HTML"
    - "Passing --target-repo pbau3r-sfdy/starflight-dynamics routes the commit to that repo"
  artifacts:
    - path: "_scripts/ingest-artifact.mjs"
      provides: "docs mode dispatch, runDocsMode function, injectDocTokens, warnMissingDocTokens, ghApiPutFile"
      contains: "modeArg === 'docs'"
  key_links:
    - from: "_scripts/ingest-artifact.mjs runDocsMode"
      to: "wiring.json brand.doc_tokens"
      via: "readJSON() + wiring?.brand?.doc_tokens"
      pattern: "doc_tokens"
    - from: "_scripts/ingest-artifact.mjs injectDocTokens"
      to: "artifact :root {} block"
      via: "fromHtml + walkTree + style text node mutation"
      pattern: "injectDocTokens"
    - from: "_scripts/ingest-artifact.mjs ghApiPutFile"
      to: "GitHub Contents API"
      via: "execSync('gh api ... --method PUT --input -')"
      pattern: "ghApiPutFile"
---

<objective>
Extend _scripts/ingest-artifact.mjs with a complete --mode docs implementation covering artifact detection, zip extraction, brand token injection, local output, GFM export, and gh api commit.

Purpose: This is the core engine of the doc pipeline. All DOCS-01/02/04/05/06 capabilities live here. The skill (Plan 03) is a thin wrapper; the script does all the work.

Output: ingest-artifact.mjs gains a runDocsMode() function and supporting helpers; operators can run --mode docs from CLI or via the skill.
</objective>

<execution_context>
@/Users/pbau3r/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pbau3r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-doc-pipeline/06-CONTEXT.md
@.planning/phases/06-doc-pipeline/06-RESEARCH.md
@.planning/phases/06-doc-pipeline/06-PATTERNS.md
@.planning/phases/06-doc-pipeline/06-01-SUMMARY.md

<interfaces>
<!-- Key signatures and line numbers from _scripts/ingest-artifact.mjs that docs mode must reuse. -->
<!-- All helpers below are already present — do NOT redefine them. -->

Reusable helpers (exact line numbers):
  extractCSSVars(cssText)         → Map<'--name','value'>       lines 99–105
  walkTree(node, callback)        → void                         lines 113–122
  extractStyleCSS(htmlString)     → string                       lines 147–159
  readJSON(p)                     → object|null                  lines 93–95
  flag(name)                      → boolean (consumes from args) lines 55–58
  option(name)                    → string|null (consumes args)  lines 59–63
  run(cmd, cwd)                   → void (DRY_RUN-aware)         lines 85–91
  log/info/ok/warn/fail/dry       → console helpers              lines 45–50, 72

Mode guard insertion point:        line 241
  current: if (modeArg !== 'full' && modeArg !== 'section')
  change to: if (modeArg !== 'full' && modeArg !== 'section' && modeArg !== 'docs')

Dispatch block insertion point:    between line 241 and line 457
  follow the section mode dispatch pattern at lines 498–505

Existing imports (lines 31–39) — add after line 39:
  import AdmZip from 'adm-zip';
  import TurndownService from 'turndown';
  (If Plan 01 SUMMARY.md says createRequire is needed, use that pattern instead)

USAGE string:                      line 74
  current: '...[--mode full|section]...'
  update to include: --mode docs|full|section, --name <n>, --format md, --target-repo org/repo, --commit, --force

Section divider comment style (non-negotiable):
  // ── Section Name ──────────────────────────────────────────────────────────────

Done banner style (lines 671–682):
  log(`\n${'═'.repeat(52)}\n ✅  ...\n${'═'.repeat(52)}\n...`);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core docs mode — artifact detection, brand token injection, local output</name>
  <files>_scripts/ingest-artifact.mjs</files>
  <read_first>
    - _scripts/ingest-artifact.mjs (read the FULL file — all 682 lines — before touching anything; understand the existing helpers, arg parsing order, mode dispatch pattern, and done banner structure)
    - .planning/phases/06-doc-pipeline/06-PATTERNS.md (all patterns — especially the mode dispatch at line 241, artifact path construction in Shared Patterns, wiring.json read pattern, and the DRY_RUN guard pattern)
    - .planning/phases/06-doc-pipeline/06-01-SUMMARY.md (check whether default import or createRequire is needed for adm-zip and turndown)
    - sites/sfdy-alt-clean/wiring.json (confirm the brand block structure — brand.doc_tokens does NOT yet exist; the field is new)
  </read_first>
  <action>
    Make the following additions to _scripts/ingest-artifact.mjs. All changes must respect the existing code style: section divider comments use `// ── Name ──────────────────` format, all console output uses the log/ok/warn/fail/dry helpers (never console.log directly in new code), all file writes are guarded by `if (!DRY_RUN)`.

    1. ADD IMPORTS after line 39 (the last existing import): add AdmZip and TurndownService using the import pattern confirmed in the Plan 01 SUMMARY.md. If SUMMARY says default import works: `import AdmZip from 'adm-zip';` and `import TurndownService from 'turndown';`. If SUMMARY says createRequire is needed, use `import { createRequire } from 'module';` then `const require = createRequire(import.meta.url);` then `const AdmZip = require('adm-zip');` and `const TurndownService = require('turndown');` — placed after the static import block. Add a comment on each: `// docs mode — zip extraction` and `// docs mode — GFM export`.

    2. UPDATE USAGE string at line 74 to: `'Usage: node _scripts/ingest-artifact.mjs <slug> [--analyze] [--mode docs|full|section] [--section <name>] [--name <n>] [--format md] [--target-repo org/repo] [--commit] [--force] [--dry-run]'`

    3. UPDATE JSDoc header (lines 7–15): add `--mode docs` to the Modes section and add a `What it does (docs mode):` block listing the 6 steps: (1) detects artifact (HTML or zip), (2) validates brand.doc_tokens, (3) injects CSS token overrides, (4) writes _captures/<slug>/docs/<name>.html, (5) optionally exports GFM Markdown, (6) commits to prod_repo via gh api when --commit is passed.

    4. UPDATE MODE GUARD at line 241: add `&& modeArg !== 'docs'` so the condition is `if (modeArg !== 'full' && modeArg !== 'section' && modeArg !== 'docs')`.

    5. ADD DOCS MODE DISPATCH after line 241 and BEFORE line 457 (the full-site artifact path read). Follow the same `if (modeArg === '...') { ...; process.exit(0); }` structure used at lines 498–505 for section mode. The dispatch parses docs-mode flags then calls runDocsMode:
       - Parse nameArg via option('--name') — default to 'index' if null
       - Parse formatArg via option('--format') — null or 'md'
       - Parse targetRepoArg via option('--target-repo') — null or 'org/repo'
       - Parse commitFlag via flag('--commit') — boolean
       - Parse forceFlag via flag('--force') — boolean
       Then call runDocsMode(slug, siteDir, { nameArg, formatArg, targetRepoArg, commitFlag, forceFlag }) then process.exit(0).

    6. ADD THREE NEW HELPER FUNCTIONS before the dispatch block insertion point (or at the bottom of the helpers section, before line 202 where the analyze flow begins). Add them with section dividers:

       FUNCTION warnMissingDocTokens(slug, siteDir):
       - Calls warn(`brand.doc_tokens not set in sites/${slug}/wiring.json`) and warn('Add this field before running --mode docs. Suggested values from Layout.astro:')
       - Reads layoutPath via `join(siteDir, 'src', 'layouts', 'Layout.astro')`
       - If layout exists: reads it, calls extractCSSVars(readFileSync(layoutPath, 'utf-8')), prints the suggested brand.doc_tokens block — format is: `log('  "brand": {')`, `log('    "doc_tokens": {')`, then for each [name, val] of vars: `log(`      "${name}": "${val}",`)`, then `log('    }')` and `log('  }')` — so operator can copy-paste into wiring.json
       - Calls process.exit(1)

       FUNCTION injectDocTokens(html, docTokens):
       - fromHtml(html) → tree; walkTree to find element nodes with tagName === 'style'
       - For each style node's text children that include ':root': for each [prop, newVal] in Object.entries(docTokens): build escapedProp by escaping regex metacharacters; build varRe = new RegExp(`(${escapedProp}\\s*:\\s*)[^;\\n]+`, 'g'); if varRe.test(css): replace with `$1${newVal}`; else: insert `\n  ${prop}: ${newVal};` after the opening `:root {` brace. Record {prop, before: existing value or null, after: newVal} in a beforeAfter array.
       - Returns { injectedHtml: toHtml(tree), beforeAfter: Array<{prop, before, after}> }

       FUNCTION runDocsMode(slug, siteDir, opts):
       This function receives { nameArg, formatArg, targetRepoArg, commitFlag, forceFlag }.

       a. VALIDATE flags:
          - If nameArg and !/^[a-z0-9-]+$/.test(nameArg): fail('--name must match ^[a-z0-9-]+$') — per D-10
          - outputName = nameArg || 'index'
          - If targetRepoArg and !/^[a-z0-9-]+\/[a-z0-9-]+$/.test(targetRepoArg): fail('--target-repo must match org/repo format')
          - If targetRepoArg && !targetRepoArg.startsWith('pbau3r-sfdy/'): warn(`--target-repo "${targetRepoArg}" is outside the pbau3r-sfdy org — proceed with caution`)

       b. READ wiring.json: `const wiring = readJSON(join(ROOT, 'sites', slug, 'wiring.json')) ?? {}`
          - docTokens = wiring?.brand?.doc_tokens (use optional chaining — brand key may be absent per RESEARCH.md Assumption A5)
          - If !docTokens || Object.keys(docTokens).length === 0: call warnMissingDocTokens(slug, siteDir) — function calls process.exit(1)
          - prodRepo = targetRepoArg || wiring?.prod_repo
          - If !prodRepo: fail(`prod_repo not set in sites/${slug}/wiring.json — pass --target-repo org/repo`)

       c. ARTIFACT DETECTION:
          - rawDir = join(ROOT, '_captures', slug, 'raw')
          - artifactPath = join(rawDir, 'artifact.html')
          - htmlPath = null
          - If existsSync(artifactPath): htmlPath = artifactPath; ok(`using artifact.html`)
          - Else: find *.zip files in rawDir via readdirSync(rawDir).filter(f => f.endsWith('.zip')) — if rawDir doesn't exist or no zip found: fail(`No artifact found at _captures/${slug}/raw/artifact.html and no .zip file in _captures/${slug}/raw/ — stage the artifact first`)
          - Take the first zip found (zipPath = join(rawDir, zipFiles[0]))
          - extractedDir = join(rawDir, 'extracted')
          - if !DRY_RUN: new AdmZip(zipPath).extractAllTo(extractedDir, true); ok(`extracted ${zipFiles[0]} to raw/extracted/`)
          - Recursively find all .html files under extractedDir (helper: findHtmlFiles(dir) using readdirSync with withFileTypes — follow the pattern from RESEARCH.md Pattern 4)
          - If htmlFiles.length === 0: fail(`No HTML file found in zip ${zipFiles[0]} — check the zip contents`)
          - If htmlFiles.length === 1: htmlPath = htmlFiles[0]; ok(`auto-selected ${basename(htmlFiles[0])}`)
          - If htmlFiles.length > 1 and !forceFlag: print numbered list via log(), then use `execSync('read reply < /dev/tty && echo $reply', { encoding: 'utf-8', stdio: ['pipe', 'pipe', 'inherit'] }).trim()` to get operator choice; parse as 1-based integer idx; if NaN or out of range: fail('Invalid choice'); htmlPath = htmlFiles[idx - 1]
          - If htmlFiles.length > 1 and forceFlag: htmlPath = htmlFiles[0]; warn(`--force: auto-selected ${basename(htmlFiles[0])} (first of ${htmlFiles.length} HTML files)`)

       d. READ and PROCESS artifact:
          - htmlString = DRY_RUN ? '<html><head><style>:root{}</style></head><body></body></html>' : readFileSync(htmlPath, 'utf-8')
          - Call injectDocTokens(htmlString, docTokens) → { injectedHtml, beforeAfter }

       e. WRITE local output:
          - docsDir = join(ROOT, '_captures', slug, 'docs')
          - outputPath = join(docsDir, outputName + '.html')
          - if !DRY_RUN: mkdirSync(docsDir, { recursive: true }); writeFileSync(outputPath, injectedHtml, 'utf-8'); ok(`wrote _captures/${slug}/docs/${outputName}.html`)
          - else: dry(`would write _captures/${slug}/docs/${outputName}.html`)

       f. PRINT SUMMARY (always, even in dry-run):
          - fileSize = DRY_RUN ? '?' : `${Math.round(Buffer.byteLength(injectedHtml, 'utf-8') / 1024)} KB`
          - log the summary block matching the D-06 format from RESEARCH.md Pattern 7:
            `── Doc Generation Summary ─────────────────────────────────`
            `  Brand tokens injected (${beforeAfter.length}):`
            for each {prop, before, after}: `    ${prop}: ${before || '(new)'}  →  ${after}`
            `  Output:           docs/${outputName}.html:  ${fileSize}`
            `  Target repo:      ${prodRepo} → docs/${outputName}.html`
            If formatArg === 'md': `  GFM export:       docs/${outputName}.md: (pending)` (size shown after write in Task 2)
            `────────────────────────────────────────────────────────────`

       g. CLEANUP extracted/ on success (Claude's discretion per CONTEXT.md): if zip was extracted and !DRY_RUN: try rmSync(extractedDir, { recursive: true }); ok('cleaned up raw/extracted/') — leave on failure for debugging. Import rmSync at the top of the file (add to the existing `import { ... } from 'fs';` block if not already present).

    At this point Task 1 is complete. The runDocsMode function ends after the summary print. The commit and GFM export (Task 2) will be added as the next block inside runDocsMode before it returns.
  </action>
  <verify>
    <automated>node /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_scripts/ingest-artifact.mjs sfdy-alt-clean --mode docs --dry-run 2>&1 | grep -i 'doc_tokens'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "modeArg === 'docs'" _scripts/ingest-artifact.mjs` returns ≥ 1
    - `grep -c "modeArg !== 'docs'" _scripts/ingest-artifact.mjs` returns ≥ 1 (mode guard updated)
    - `grep -c 'runDocsMode' _scripts/ingest-artifact.mjs` returns ≥ 2 (definition + call)
    - `grep -c 'injectDocTokens' _scripts/ingest-artifact.mjs` returns ≥ 2 (definition + call)
    - `grep -c 'warnMissingDocTokens' _scripts/ingest-artifact.mjs` returns ≥ 2 (definition + call)
    - `grep -c 'doc_tokens' _scripts/ingest-artifact.mjs` returns ≥ 3 (wiring access + warn message + summary print)
    - `node _scripts/ingest-artifact.mjs sfdy-alt-clean --mode docs --dry-run 2>&1 | grep -i 'doc_tokens'` exits 0 (script dispatches to docs mode and hits the brand.doc_tokens validation, printing the expected warning)
    - `grep -c 'adm-zip\|AdmZip' _scripts/ingest-artifact.mjs` returns ≥ 2 (import + usage in zip extraction)
  </acceptance_criteria>
  <done>Docs mode dispatches correctly, artifact detection (HTML and zip path) implemented, brand.doc_tokens validation with D-02 warning implemented, CSS token injection implemented, local output written, D-06 summary printed to console</done>
</task>

<task type="auto">
  <name>Task 2: Commit and export layer — gh api PUT, --target-repo, GFM export, done banner</name>
  <files>_scripts/ingest-artifact.mjs</files>
  <read_first>
    - _scripts/ingest-artifact.mjs (current state after Task 1 — read from the runDocsMode function section to see where to add the new blocks)
    - .planning/phases/06-doc-pipeline/06-RESEARCH.md (Pattern 3: ghApiPutFile with SHA fetch; Pattern 5: htmlToGfm with TurndownService; Common Pitfall 1: SHA required for updates; Pitfall 2: CJS import in ESM; Security Domain: shell injection mitigation via --input -)
  </read_first>
  <action>
    Extend _scripts/ingest-artifact.mjs with the commit and GFM export capabilities. All changes go inside runDocsMode, appended after the summary print block from Task 1, and a new ghApiPutFile helper is added as a named function (with a section divider comment) near the other helpers.

    1. ADD FUNCTION ghApiPutFile(repoFullName, repoPath, fileBytes, commitMessage) following RESEARCH.md Pattern 3 exactly:
       - Split repoFullName into [owner, repo]
       - apiPath = `repos/${owner}/${repo}/contents/${repoPath}`
       - SHA fetch: try `execSync(\`gh api ${apiPath} --jq .sha\`, { encoding: 'utf-8' }).trim()` — catch (file does not exist) → sha = null. If result is truthy and !== 'null': sha = result.
       - Build body = JSON.stringify({ message: commitMessage, content: fileBytes.toString('base64'), ...(sha ? { sha } : {}) })
       - Commit: `execSync(\`gh api ${apiPath} --method PUT --input -\`, { input: body, stdio: ['pipe', 'inherit', 'inherit'] })`
       - CRITICAL: never shell-interpolate the base64 string — always use --input - stdin body (Security: prevents shell injection and avoids arg length limits)

    2. INSIDE runDocsMode, AFTER the summary print block, add GFM EXPORT block:
       - gfmOutputPath = null
       - If formatArg === 'md' and !DRY_RUN:
         - Extract body content: bodyMatch = injectedHtml.match(/<body[^>]*>([\s\S]*?)<\/body>/i); bodyContent = bodyMatch ? bodyMatch[1] : injectedHtml
         - td = new TurndownService({ headingStyle: 'atx', bulletListMarker: '-', codeBlockStyle: 'fenced' })
         - gfmContent = td.turndown(bodyContent)
         - gfmOutputPath = join(docsDir, outputName + '.md')
         - writeFileSync(gfmOutputPath, gfmContent, 'utf-8'); ok(`wrote _captures/${slug}/docs/${outputName}.md`)
       - If formatArg === 'md' and DRY_RUN: dry(`would write _captures/${slug}/docs/${outputName}.md`)

    3. INSIDE runDocsMode, AFTER the GFM export block, add COMMIT block:
       - If commitFlag and !DRY_RUN:
         - ok(`committing docs/${outputName}.html to ${prodRepo}...`)
         - htmlBytes = readFileSync(outputPath)
         - ghApiPutFile(prodRepo, `docs/${outputName}.html`, htmlBytes, `docs: add ${outputName}.html [wm-gen-docs]`)
         - ok(`committed docs/${outputName}.html to github.com/${prodRepo}`)
         - If gfmOutputPath:
           - gfmBytes = readFileSync(gfmOutputPath)
           - ghApiPutFile(prodRepo, `docs/${outputName}.md`, gfmBytes, `docs: add ${outputName}.md [wm-gen-docs]`)
           - ok(`committed docs/${outputName}.md to github.com/${prodRepo}`)
       - If commitFlag and DRY_RUN: dry(`would call gh api PUT → ${prodRepo}/docs/${outputName}.html`)

    4. AFTER the commit block, add DONE BANNER using the ═ border style from lines 671–682:
       - If commitFlag: banner shows "Committed to github.com/${prodRepo}/blob/main/docs/${outputName}.html" and if GFM export: "and docs/${outputName}.md"
       - If !commitFlag: banner shows "Staged at _captures/${slug}/docs/${outputName}.html — run with --commit to push to ${prodRepo}"
       - Use log(`\n${'═'.repeat(52)}\n ✅  docs/${outputName}.html generated for ${slug}.\n${'═'.repeat(52)}\n...`)

    5. UPDATE the done banner in the docs mode to also mention --format md and --target-repo flags in the "Next steps" section so operators know the full API.
  </action>
  <verify>
    <automated>grep -c 'ghApiPutFile' /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_scripts/ingest-artifact.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ghApiPutFile' _scripts/ingest-artifact.mjs` returns ≥ 2 (function definition + call inside runDocsMode)
    - `grep -c 'TurndownService' _scripts/ingest-artifact.mjs` returns ≥ 2 (import/require + usage in GFM block)
    - `grep -c '\-\-input -' _scripts/ingest-artifact.mjs` returns ≥ 1 (SHA injection prevention — confirming the stdin PUT pattern is used)
    - `grep -c '\.sha' _scripts/ingest-artifact.mjs` returns ≥ 1 (SHA fetch before PUT — pitfall prevention)
    - `grep -c 'commitFlag' _scripts/ingest-artifact.mjs` returns ≥ 2 (flag parsed + checked before gh api call)
    - `node _scripts/ingest-artifact.mjs sfdy-alt-clean --mode docs --commit --dry-run 2>&1 | grep '\[dry\]'` exits 0 and prints at least one [dry] line (confirming DRY_RUN guard is respected when --commit is passed)
    - `node _scripts/ingest-artifact.mjs sfdy-alt-clean --mode docs --dry-run 2>&1 | grep -i 'doc_tokens'` still exits 0 (prior Task 1 behavior unchanged)
  </acceptance_criteria>
  <done>ghApiPutFile function implemented with SHA fetch and --input - stdin; GFM export via TurndownService; --commit flag gates the gh api call; done banner printed; DRY_RUN guard respected for all new writes and API calls</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → script flags | --name, --target-repo, --format, --commit are operator-supplied; validated before any file write or API call |
| script → GitHub Contents API | gh api PUT commits files to prod repos; auth via gh CLI keyring session |
| zip artifact → file system | adm-zip extracts operator-supplied zip; path traversal (zip slip) possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | --name flag in runDocsMode | mitigate | validate against `^[a-z0-9-]+$` before any file write (D-10); fail() immediately if pattern does not match |
| T-06-02 | Tampering | --target-repo flag | mitigate | validate against `^[a-z0-9-]+/[a-z0-9-]+$` before any file write; warn if not prefixed with pbau3r-sfdy/ |
| T-06-03 | Tampering | gh api PUT in ghApiPutFile | mitigate | base64 content passed via `--input -` stdin JSON body (never shell-interpolated); prevents command injection and shell arg length limits |
| T-06-04 | Elevation of Privilege | adm-zip extraction (zip slip) | mitigate | after extraction, verify all found HTML paths start with extractedDir (join(rawDir, 'extracted')) — reject and fail if any path escapes the extractedDir prefix |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | blocked on Plan 01 human checkpoint; adm-zip and turndown confirmed [ASSUMED] → verified → installed before this plan runs |
</threat_model>

<verification>
After both tasks complete:
- `node _scripts/ingest-artifact.mjs sfdy-alt-clean --mode docs --dry-run 2>&1 | grep -i 'doc_tokens'` exits 0 (mode dispatches, brand validation runs)
- `node _scripts/ingest-artifact.mjs sfdy-alt-clean --mode docs --commit --dry-run 2>&1 | grep '\[dry\]'` exits 0 (commit gate respected in dry-run)
- `grep -c 'ghApiPutFile\|injectDocTokens\|warnMissingDocTokens\|runDocsMode' _scripts/ingest-artifact.mjs` returns ≥ 8 (all four functions present with calls)
- `grep -c '\-\-input -' _scripts/ingest-artifact.mjs` returns ≥ 1 (shell injection prevention in place)
</verification>

<success_criteria>
ingest-artifact.mjs supports --mode docs end-to-end: artifact detection (HTML + zip), brand.doc_tokens validation, CSS token injection, local output, GFM export, gh api PUT commit — all DOCS-01/02/04/05/06 capabilities are present and verified.
</success_criteria>

<output>
Create `.planning/phases/06-doc-pipeline/06-02-SUMMARY.md` when done. Include: exact line range of the runDocsMode function, the ghApiPutFile function location, and the adm-zip import pattern used (default or createRequire) — Plan 03 skill needs to reference the correct script flags.
</output>
