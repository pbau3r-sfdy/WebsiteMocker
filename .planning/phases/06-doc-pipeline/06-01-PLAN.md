---
phase: 06-doc-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: false
requirements:
  - DOCS-01
  - DOCS-06
must_haves:
  truths:
    - "adm-zip is confirmed as a legitimate package (npmjs.com: author cthackers, created ~2012, MIT, no postinstall)"
    - "turndown is confirmed as a legitimate package (npmjs.com: author domchristie/mixmark-io, created ~2017, MIT, no postinstall)"
    - "Both packages are installed in node_modules at repo root and importable from ESM .mjs files via default import"
  artifacts:
    - path: "node_modules/adm-zip/package.json"
      provides: "adm-zip installed"
    - path: "node_modules/turndown/package.json"
      provides: "turndown installed"
  key_links:
    - from: "_scripts/ingest-artifact.mjs"
      to: "adm-zip"
      via: "import AdmZip from 'adm-zip'"
      pattern: "adm-zip"
    - from: "_scripts/ingest-artifact.mjs"
      to: "turndown"
      via: "import TurndownService from 'turndown'"
      pattern: "turndown"
---

<objective>
Verify and install the two npm packages required for docs mode before any script work begins.

Purpose: RESEARCH.md tags adm-zip and turndown as [ASSUMED] because slopcheck was unavailable — both must be manually confirmed on npmjs.com before install. This plan is a hard gate: Plan 02 cannot proceed until these packages are installed and importable.

Output: adm-zip@^0.6.0 (zip extraction for DOCS-01) and turndown@^7.2.4 (GFM export for DOCS-06) installed at project root and importable from Node.js ESM .mjs files.
</objective>

<execution_context>
@/Users/pbau3r/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pbau3r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-doc-pipeline/06-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify adm-zip and turndown packages on npmjs.com before install</name>
  <read_first>
    - .planning/phases/06-doc-pipeline/06-RESEARCH.md (Package Legitimacy Audit section — both entries are [ASSUMED]; Common Pitfalls section — Pitfall 2 and 3 for CJS import in ESM)
  </read_first>
  <what-built>
    adm-zip and turndown are listed as [ASSUMED] in the RESEARCH.md Package Legitimacy Audit because slopcheck could not run at research time. Each package must be confirmed on the npm registry before install.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/adm-zip in a browser.
       Check: package exists; maintainer shows "cthackers" (source: github.com/cthackers/adm-zip); version history goes back to ~2012; weekly downloads show active usage; license is MIT; no postinstall script listed in the Dist Tags or Code tab.
    2. Open https://www.npmjs.com/package/turndown in a browser.
       Check: package exists; maintainer shows "domchristie" or org "mixmark-io" (source: github.com/mixmark-io/turndown); version history goes back to ~2017; weekly downloads show active usage; license is MIT; no postinstall script listed.
    3. If either package shows an unexpected author, very few downloads, or a postinstall script entry, stop and describe the concern — do NOT type "verified".
  </how-to-verify>
  <acceptance_criteria>
    - adm-zip: package page loads on npmjs.com, maintainer matches "cthackers", first publish year ≤ 2013, no postinstall script visible
    - turndown: package page loads on npmjs.com, maintainer matches "domchristie" or "mixmark-io", first publish year ≤ 2018, no postinstall script visible
  </acceptance_criteria>
  <resume-signal>Type "verified" if both packages pass inspection, or describe the concern found.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install adm-zip and turndown at repo root</name>
  <files>package.json</files>
  <read_first>
    - package.json (repo root — read current "dependencies" and "devDependencies" blocks to see existing package entries and confirm the install target)
  </read_first>
  <action>
    Run `npm install adm-zip@^0.6.0 turndown@^7.2.4` at the repo root (/Users/pbau3r/DevWorks/Websites/WebsiteMocker). This writes both packages into root package.json (under "dependencies") and into node_modules/.

    After npm install completes, verify that both packages can be imported using the CJS default import pattern in Node.js ESM — this is RESEARCH.md Assumption A1/A2 and must be confirmed before Plan 02 proceeds:

    Run: `node --input-type=module -e "import AdmZip from 'adm-zip'; console.log('AdmZip:', typeof AdmZip)"` from /Users/pbau3r/DevWorks/Websites/WebsiteMocker. Expected output: `AdmZip: function`.

    Run: `node --input-type=module -e "import TurndownService from 'turndown'; console.log('TurndownService:', typeof TurndownService)"` from the same directory. Expected output: `TurndownService: function`.

    If either command prints "undefined" or throws ERR_REQUIRE_ESM or "is not a constructor": record in the SUMMARY.md that Plan 02 must use createRequire fallback for that package: `import { createRequire } from 'module'; const require = createRequire(import.meta.url); const AdmZip = require('adm-zip');`. This is documented in PATTERNS.md (package.json section, CJS fallback note).

    The import test result (default import OK, or createRequire needed) MUST be recorded in the plan summary so the Plan 02 executor knows which pattern to use.
  </action>
  <verify>
    <automated>node --input-type=module -e "import AdmZip from 'adm-zip'; import TurndownService from 'turndown'; console.log(typeof AdmZip, typeof TurndownService)" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - package.json at repo root contains "adm-zip" key in "dependencies" or "devDependencies"
    - package.json at repo root contains "turndown" key in "dependencies" or "devDependencies"
    - node_modules/adm-zip/package.json exists
    - node_modules/turndown/package.json exists
    - `node --input-type=module -e "import AdmZip from 'adm-zip'; console.log(typeof AdmZip)"` prints "function" (confirming A1) OR SUMMARY.md notes createRequire fallback is required
    - `node --input-type=module -e "import TurndownService from 'turndown'; console.log(typeof TurndownService)"` prints "function" (confirming A2) OR SUMMARY.md notes createRequire fallback is required
  </acceptance_criteria>
  <done>adm-zip@^0.6.0 and turndown@^7.2.4 installed in node_modules and importable from Node.js ESM; import pattern (default or createRequire) recorded in summary for Plan 02</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → npm registry | npm install fetches packages from registry; [ASSUMED] packages require operator confirmation before install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-SC | Tampering | npm install (adm-zip, turndown) | mitigate | blocking human checkpoint in Task 1 — operator visits npmjs.com/package/adm-zip and npmjs.com/package/turndown to confirm authorship, age, download volume, and absence of postinstall scripts before Task 2 runs |
</threat_model>

<verification>
After both tasks complete:
- `ls node_modules/adm-zip/package.json node_modules/turndown/package.json` exits 0
- `grep -c '"adm-zip"' package.json` returns ≥ 1
- `grep -c '"turndown"' package.json` returns ≥ 1
- Import pattern (default or createRequire) is recorded in SUMMARY.md for Plan 02 executor
</verification>

<success_criteria>
Both packages verified as legitimate, installed at repo root, and confirmed importable from ESM .mjs files. Plan 02 can begin script extension work without import failures.
</success_criteria>

<output>
Create `.planning/phases/06-doc-pipeline/06-01-SUMMARY.md` when done. CRITICAL: record the import test result (default import works / createRequire required) for each package in the summary — Plan 02 depends on this.
</output>
