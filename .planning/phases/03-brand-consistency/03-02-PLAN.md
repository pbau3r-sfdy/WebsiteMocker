---
phase: 03-brand-consistency
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - .claude/skills/wm-wire.md
autonomous: false
requirements: [BRAND-02]

must_haves:
  truths:
    - "Running /wm-wire on a site with no brand block (or empty stubs) offers to build one interactively"
    - "The skill generates a pre-filled JSON template and instructs operator to take it to Claude.ai"
    - "Running /wm-wire on a site with an existing non-empty brand block applies a recency check — same-day context skips the 'has anything changed?' prompt"
    - "Skip and Not needed both allow stage advancement — brand block is never a gate"
    - "mogwai-systems (capture: null) is handled gracefully — skill reads available signals and skips absent ones without error"
  artifacts:
    - path: ".claude/skills/wm-wire.md"
      provides: "Brand block service section within step 3"
      contains: "### Brand block"
  key_links:
    - from: "wm-wire.md Brand block section"
      to: "sites/<slug>/wiring.json brand key"
      via: "skill writes validated JSON on 'Configure now' path"
      pattern: "Configure now"
    - from: "wm-wire.md recency check"
      to: "wiring.json last_deploy field"
      via: "same-day date comparison"
      pattern: "last_deploy"
---

## Phase Goal

**As a** site operator, **I want to** configure a brand block interactively via /wm-wire using a Claude.ai-assisted artifact, **so that** I can define my brand identity once and have it available to content skills.

<objective>
Extend wm-wire.md with a "Brand block" service section that fits naturally as the fifth service (after Domain) in the existing wizard. The section implements the three-way prompt (Configure now / Skip for later / Not needed), an artifact-based first-run workflow, a recency-checked update path, and graceful handling of sites with no capture data.

Purpose: The operator can establish a brand block for any site by running /wm-wire and following the prompts. No terminal interview — the skill generates a pre-filled template the operator takes to Claude.ai, fills with Claude's help, and pastes back. One artifact, one fill.

Output: Modified .claude/skills/wm-wire.md with Brand block section; human verification confirms the skill behaves correctly on mogwai-systems (no capture data).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pbau3r/DevWorks/Websites/WebsiteMocker/.planning/ROADMAP.md
@/Users/pbau3r/DevWorks/Websites/WebsiteMocker/.planning/phases/03-brand-consistency/03-CONTEXT.md
@/Users/pbau3r/DevWorks/Websites/WebsiteMocker/.planning/phases/03-brand-consistency/03-RESEARCH.md
@/Users/pbau3r/DevWorks/Websites/WebsiteMocker/.planning/phases/03-brand-consistency/03-PATTERNS.md

<interfaces>
From .claude/skills/wm-wire.md (current structure, read in full before editing):
  Step 3 walks four services: Newsletter → Contact form → Social handles → Domain
  Step 4 advances stage if all services configured/skipped
  Step 5 commits with message "feat(<slug>): wire services"
  Step 6 reports

  Notes section pattern:
    "Skip for later" leaves the field null — not counted against stage advancement
    "Not needed" sets status: "skipped" — counts as done for stage purposes

From sites/mogwai-systems/wiring.json (no capture — the key test case):
  capture: null  ← skip all capture-reading logic for this site
  No keywords.json in sites/mogwai-systems/ per research audit
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Brand block service section to wm-wire.md</name>
  <files>.claude/skills/wm-wire.md</files>
  <read_first>
    - .claude/skills/wm-wire.md — read in full; identify the exact end of the "### Domain" subsection and the start of step 4 (stage advance); this is the insertion point
    - .planning/phases/03-brand-consistency/03-PATTERNS.md — "BRAND-02: .claude/skills/wm-wire.md" section; Brand block section template; placement reference at bottom of file
    - .planning/phases/03-brand-consistency/03-CONTEXT.md — /wm-wire UX decisions: artifact workflow, recency check threshold, three-way prompt
    - .planning/phases/03-brand-consistency/03-RESEARCH.md — Pitfall 1 (stage advance must not be blocked), Pitfall 4 (paste-back validation), Pre-Fill Signal Inventory table
  </read_first>
  <action>
    Insert a "### Brand block" subsection within step 3 of wm-wire.md, after the "### Domain" subsection and before step 4 (stage advance). The heading must read exactly: "### Brand block". Brand block is a peer service — it is the fifth entry in step 3's service list, not a new top-level step.

    The section must implement four paths under the three-way prompt "Configure now, skip for later, or mark as not needed?":

    PATH A — "Configure now", first-run (no brand block key OR all four brand sub-keys are empty arrays / empty string):
    - Read available signals: (1) wiring.json "name" and "domain" — always present; (2) wiring.json "capture" field — if non-null, read _captures/<capture-value>/CAPTURE.md and/or capture.json for hashtags and vocabulary signals; if "capture" is null, skip this source entirely without error; (3) sites/<slug>/keywords.json — if the file exists, read brand.avoid, hashtags.twitter, hashtags.linkedin, hashtags.instagram, and primary/secondary terms; if the file does not exist, skip without error; (4) any existing content files in src/content/**/*.md — scan tags[] arrays for recurring hashtag signals.
    - Using extracted signals, generate a pre-filled JSON template with four keys: "hashtags" (array from capture hashtags and keywords.json hashtag fields, deduplicated, without # prefix), "vocabulary" (array from keywords.json primary/secondary terms and prominent terminology in capture text), "avoid" (array from keywords.json brand.avoid if present, empty array if absent), "voice" (a one-sentence descriptor inferred from the site name and captured page tone, or an empty string if no signals are available).
    - Print the template to the operator with this exact instruction line: "Here is your brand kit template for [name] — pre-filled with signals from the codebase. Take this to Claude.ai and ask Claude to help you refine and complete it. Then paste the finished JSON back here."
    - Print the template as a fenced JSON block containing only the four brand fields (hashtags, vocabulary, avoid, voice) — not the full wiring.json.
    - Wait for the operator to paste the completed JSON back.
    - Validate the pasted JSON before writing: (1) confirm exactly four keys are present: hashtags, vocabulary, avoid, voice; (2) hashtags, vocabulary, and avoid must be JSON arrays (even if empty); (3) voice must be a string. If any field fails validation, report the exact field name and the expected type, then wait for a corrected paste. Do not write until validation passes.
    - Write the validated object as the value of the "brand" key in wiring.json (replacing the empty stub if one exists).

    PATH B — "Configure now", subsequent-run (brand block exists AND at least one field is non-empty):
    - Read wiring.json "last_deploy" field. Compare it to today's date as a YYYY-MM-DD string.
    - If last_deploy matches today: skip the "has anything changed?" prompt; tell the operator what fields are currently set and proceed directly to any field update they request.
    - If last_deploy is older than today (or absent): ask "Has anything in your brand voice or hashtags changed since you last ran /wm-wire?" Apply any changes the operator provides and write the updated brand block.

    PATH C — "Skip for later": leave the brand key as-is (empty stub from plan 03-01, or absent). Do not block stage advancement.

    PATH D — "Not needed": set brand block to an object with a single key "status" set to the string "skipped". Counts as done for stage advancement purposes, consistent with the existing "Not needed" behaviour for other services.

    After the Brand block subsection, update step 4's stage-advance note: append an explicit line stating that the brand block does NOT gate stage advancement — Skip for later and Not needed both count as done, and a site with an empty brand stub also counts.

    In the existing Notes section at the bottom of the file, add a new note: "Brand block is optional enrichment — sites without a brand block (or with all-empty arrays) get no brand-aware behaviour in content skills; /wm-wire is not required before using content skills."

    Update step 5 (commit) to conditionally include wiring.json: if the brand block was written or updated during this run, include sites/<slug>/wiring.json in the git add command. The commit message pattern "feat(<slug>): wire services" remains unchanged.

    After editing, commit the file:
    git add .claude/skills/wm-wire.md
    git commit -m "feat(wm-wire): add Brand block service section (BRAND-02)"
    git push
  </action>
  <verify>
    <automated>
      grep -q "### Brand block" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/.claude/skills/wm-wire.md
      grep -q "Configure now, skip for later" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/.claude/skills/wm-wire.md
      grep -q "Take this to Claude.ai" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/.claude/skills/wm-wire.md
      grep -q "has anything changed" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/.claude/skills/wm-wire.md
      grep -qE "(does not gate stage|NOT gate stage|not required for stage)" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/.claude/skills/wm-wire.md
    </automated>
  </verify>
  <acceptance_criteria>
    - wm-wire.md contains a "### Brand block" heading within step 3, after "### Domain"
    - The section contains the three-way prompt text "Configure now, skip for later, or mark as not needed"
    - The first-run path contains the instruction "Take this to Claude.ai and ask Claude to help you refine and complete it. Then paste the finished JSON back here."
    - The first-run path explicitly names the four signal sources: wiring.json name/domain, capture DNA (conditional on capture != null), keywords.json (conditional on file existence), existing content tags
    - The first-run path includes a paste-back validation step checking for exactly four keys (hashtags, vocabulary, avoid, voice) with correct types before writing
    - The recency-check path contains "Has anything in your brand voice or hashtags changed" (or equivalent wording) for the older-than-today branch
    - The recency-check path mentions comparing last_deploy to today's date
    - Step 4 stage-advance note explicitly states brand block does not gate stage advancement
    - Notes section contains a statement that brand block is optional enrichment
    - Step 5 commit conditionally includes wiring.json when brand block was written/updated
  </acceptance_criteria>
  <done>wm-wire.md extended with complete Brand block service section covering first-run, subsequent-run, skip, and not-needed paths; committed and pushed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Extended wm-wire.md with a Brand block service section. The skill now offers to configure, skip, or mark brand blocks as not needed during /wm-wire. "Configure now" generates a pre-filled JSON template from available signals and prompts the operator to take it to Claude.ai for refinement before pasting back.</what-built>
  <how-to-verify>
    Open a new Claude session and invoke /wm-wire. When prompted for the site, enter "mogwai-systems" (this site has capture: null and no keywords.json — the minimal signal case).

    Verify each of the following:
    1. The skill reaches the "### Brand block" prompt (Configure now / Skip for later / Not needed)
    2. Choose "Configure now". Verify Claude reads only wiring.json name ("MOGWAI Systems") and domain — it does NOT try to read a capture file or error because capture is null
    3. Verify Claude generates a pre-filled JSON template with the four required keys (hashtags, vocabulary, avoid, voice) — values may be sparse/empty given minimal signals, which is correct
    4. Verify the template is printed with the instruction: "Take this to Claude.ai and ask Claude to help you refine and complete it. Then paste the finished JSON back here."
    5. Paste back a minimal valid JSON: { "hashtags": ["MOGWAI"], "vocabulary": ["autonomous systems"], "avoid": [], "voice": "Minimal, operational, no hype" }
    6. Verify Claude validates and accepts the paste, then writes the brand block to mogwai-systems/wiring.json
    7. Restart and run /wm-wire on mogwai-systems again, choosing "Configure now". Verify the recency check fires: if run today, Claude skips the "has anything changed?" prompt and shows current fields directly
    8. Run /wm-wire on parrot-capital, choose "Skip for later" for Brand block — verify stage advancement is NOT blocked

    Optionally revert mogwai-systems/wiring.json brand block back to the empty stub after verification if you do not want the test data committed.
  </how-to-verify>
  <resume-signal>Type "verified" if all eight steps pass, or describe which step failed and what happened</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator paste → wm-wire validation | Operator pastes JSON from Claude.ai; skill validates before writing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | Paste-back JSON validation in wm-wire | mitigate | Skill instruction requires validation of all four keys and correct types before writing; operator is told exactly what to fix on failure |
| T-03-02-02 | Spoofing | wm-wire reading capture/keywords.json | accept | All signal sources are developer-controlled files in the same repo; no external or user-controlled input |
</threat_model>

<verification>
wm-wire.md contains the Brand block section with all required paths. Human checkpoint confirms the skill behaves correctly on mogwai-systems (no capture data) and does not block stage advancement when skipped.

Automated pre-check:
grep -c "### Brand block" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/.claude/skills/wm-wire.md
</verification>

<success_criteria>
- wm-wire.md contains "### Brand block" section after "### Domain" within step 3
- First-run path generates pre-filled template and waits for Claude.ai paste-back
- Paste-back validation rejects malformed JSON and reports the exact fix needed
- Recency check skips "has anything changed?" prompt when last_deploy matches today
- Skip for later and Not needed both allow stage advancement
- Human checkpoint: operator verifies mogwai-systems flow and recency check work correctly
</success_criteria>

<output>
Create /Users/pbau3r/DevWorks/Websites/WebsiteMocker/.planning/phases/03-brand-consistency/03-02-SUMMARY.md when done
</output>
