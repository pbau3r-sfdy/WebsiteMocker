---
phase: 01-production-deploy-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .claude/skills/wm-publish.md
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-07

must_haves:
  truths:
    - "Running /wm-publish <slug> in Claude reads wiring.json, checks stage/domain/prod_repo, runs /wm-preflight, and blocks on any FAIL items before triggering the workflow"
    - "Running /wm-publish <slug> triggers publish.yml via: gh workflow run publish.yml --field slug=<slug>"
    - "Running /wm-publish <slug> waits for workflow completion using gh run watch and surfaces exit code"
    - "After a successful publish, /wm-publish prints an inline Squarespace DNS guide with: CNAME record, 4 apex A records, CAA check note, SSL provisioning wait, default-record-deletion warning"
    - "If publish.yml fails, /wm-publish prints the workflow run URL so the operator can inspect the failure log"
    - "The E2E system delivers a live site at the production domain with CNAME + robots.txt Allow + wiring.json stage 6 after a complete run"
  artifacts:
    - path: ".claude/skills/wm-publish.md"
      provides: "Operator-facing publish skill"
      contains: "wiring.json validation, /wm-preflight invocation, gh workflow run publish.yml, gh run watch, DNS guide section"
  key_links:
    - from: ".claude/skills/wm-publish.md Step 3"
      to: ".github/workflows/publish.yml"
      via: "gh workflow run publish.yml --field slug=<slug>"
      pattern: "gh workflow run publish.yml"
    - from: ".claude/skills/wm-publish.md Step 4"
      to: "gh-pages branch of prod repo"
      via: "gh run watch polling for workflow completion"
      pattern: "gh run watch"
---

<objective>
Create the operator-facing Claude skill that wraps the production deploy pipeline, and verify the complete end-to-end system works on a real site.

Purpose: The skill is what the operator actually types. It provides fail-fast validation, progress visibility, and the inline Squarespace DNS handoff guide (DEPLOY-07). Without this skill, the deploy pipeline exists but has no safe, documented entry point.

Output: `.claude/skills/wm-publish.md` (complete skill with all steps and DNS guide), plus human verification that a real deploy succeeds end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-production-deploy-pipeline/01-CONTEXT.md
@.planning/phases/01-production-deploy-pipeline/01-RESEARCH.md
@.planning/phases/01-production-deploy-pipeline/01-01-SUMMARY.md

<interfaces>
<!-- Key contracts from Plan 01 output and existing skills. -->

From .claude/skills/wm-deploy.md (skill structure pattern):
- Skill file starts with # /skill-name heading
- Contains ## Steps section with numbered steps
- Steps reference bash commands in fenced code blocks
- Short ## Notes section at the end for edge cases

From .github/workflows/publish.yml (created in Plan 01):
- Triggered via: gh workflow run publish.yml --field slug=<slug>
- Input: slug (string, required)
- Exits non-zero if: stage < 5, domain missing, prod_repo missing
- On success: CNAME written, robots.txt swapped, wiring.json committed to stage 6

From .claude/skills/wm-preflight.md (invoked by /wm-publish before triggering):
- Run against a site to get PASS/FAIL/WARN for build, content, wiring, legal, keywords
- Blocks on any FAIL items (stage >= 5, domain, prod_repo are preflight checks)

From .planning/phases/01-production-deploy-pipeline/01-RESEARCH.md §4 (skill step sequence):
- Step 1: Read wiring.json — confirm stage >= 5, domain, prod_repo
- Step 2: Run /wm-preflight <slug> — block on FAIL
- Step 3: gh workflow run publish.yml --field slug=<slug>
- Step 4: gh run watch <run-id> (with brief sleep/retry for run to appear)
- Step 5: On success → print DNS guide + live URL
- Step 6: On failure → print workflow run URL for log inspection

GitHub Pages IP addresses (from RESEARCH.md §8):
- 185.199.108.153
- 185.199.109.153
- 185.199.110.153
- 185.199.111.153
- CNAME target format: pbau3r-sfdy.github.io
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create .claude/skills/wm-publish.md</name>
  <files>.claude/skills/wm-publish.md</files>

  <read_first>
    - `.claude/skills/wm-deploy.md` — Existing skill structure: heading, ## Steps, ## Notes pattern
    - `.claude/skills/wm-preflight.md` — Preflight skill to understand what it checks and how to invoke it
    - `.planning/phases/01-production-deploy-pipeline/01-RESEARCH.md` — Section 4 (skill structure) and Section 8 (DNS IP addresses)
    - `.planning/phases/01-production-deploy-pipeline/01-CONTEXT.md` — Decisions D-01, D-02, D-10 for exact behavior
  </read_first>

  <action>
Create `.claude/skills/wm-publish.md`. Follow the exact structure of `wm-deploy.md` (heading → Steps → Notes). The file must contain:

**Heading:**
```
# /wm-publish

Build and publish a stage-5 site to its production GitHub Pages URL.
```

**## Steps** (6 numbered steps):

**Step 1 — Pre-flight validation (D-02):**
Read `sites/<slug>/wiring.json`. Confirm all three fields are set:
- `stage >= 5` — if not, stop with: "Error: site <slug> is stage X — must be stage 5 before publishing"
- `domain` — if missing, stop with: "Error: domain not set in sites/<slug>/wiring.json"
- `prod_repo` — if missing, stop with: "Error: prod_repo not set in sites/<slug>/wiring.json"

**Step 2 — Run preflight checklist (D-02):**
Run `/wm-preflight <slug>`. Present the full preflight output. If any item is FAIL, stop and list the failures — do not proceed to Step 3 until all FAIL items are resolved.

**Step 3 — Trigger publish workflow (D-01):**
```bash
gh workflow run publish.yml --field slug=<slug>
```
If this command fails (e.g. `gh` not authenticated, workflow not found), surface the exact error and stop.

**Step 4 — Wait for completion (RESEARCH.md §4 timing note):**
After triggering, wait 3 seconds for the run to appear in the list, then retrieve the run ID and watch:
```bash
sleep 3
RUN_ID=$(gh run list --workflow publish.yml --limit 1 --json databaseId -q '.[0].databaseId')
gh run watch "$RUN_ID" --exit-status
```
Stream live output to the operator. `gh run watch --exit-status` exits with the workflow's exit code.

**Step 5 — On success (D-10 inline DNS guide):**
If `gh run watch` exits 0, print:

```
--- Publish complete ---

Site: <slug>
Live URL: https://<domain>

--- Squarespace DNS Handoff Guide ---

Add these records in Squarespace Domains > DNS Settings:

1. CNAME record (www subdomain):
   Host: www
   Points to: pbau3r-sfdy.github.io
   TTL: Automatic

2. Apex A records (for root domain, e.g. example.com):
   Host: @
   Points to: 185.199.108.153
   Host: @
   Points to: 185.199.109.153
   Host: @
   Points to: 185.199.110.153
   Host: @
   Points to: 185.199.111.153
   TTL: Automatic

3. CAA record check:
   If the domain has any CAA records, ensure letsencrypt.org is listed.
   Squarespace-hosted domains typically have no CAA records — no action needed.

4. SSL provisioning:
   GitHub Pages provisions SSL via Let's Encrypt within 15–30 minutes.
   Do NOT enable "Secure" / HTTPS redirect in Squarespace until SSL is active —
   enabling it before the certificate is ready will show a security error.

5. Default record deletion (IMPORTANT):
   Delete any existing Squarespace-default A records or CNAME records pointing
   to Squarespace servers (typically 198.185.159.x or similar) before saving
   the new records. Leaving both causes DNS conflicts and intermittent 404s.

wiring.json has been automatically updated to stage: 6.
```

**Step 6 — On failure:**
If `gh run watch` exits non-zero, print:
```
Publish failed. Inspect the workflow log:
gh run view "$RUN_ID" --log-failed
```
Do not update wiring.json manually — the workflow only commits back on success.

**## Notes:**
- Only works for sites at stage 5 (all preflight checks must pass)
- `WM_PUBLISH_PAT` must be stored as a repo-level Actions secret before this skill can succeed
- Check workflow status anytime: `gh run list --workflow publish.yml --limit 5`
- Re-deploying an already-live site (stage 6) is safe — publish.yml is idempotent; CNAME and robots.txt are re-written on every run
  </action>

  <verify>
    <automated>
      test -f .claude/skills/wm-publish.md && echo "PASS: file exists" || echo "FAIL: file missing"
      grep -c "gh workflow run publish.yml" .claude/skills/wm-publish.md
      grep -c "gh run watch" .claude/skills/wm-publish.md
      grep -c "185.199.108.153" .claude/skills/wm-publish.md
      grep -c "letsencrypt" .claude/skills/wm-publish.md
      grep -c "wm-preflight" .claude/skills/wm-publish.md
      grep -c "stage.*5\|stage >= 5\|stage >=5" .claude/skills/wm-publish.md
    </automated>
  </verify>

  <acceptance_criteria>
    - File exists at `.claude/skills/wm-publish.md`
    - Contains `gh workflow run publish.yml --field slug=` (exact trigger command from D-01)
    - Contains `gh run watch` with `--exit-status` flag
    - Contains all four GitHub Pages apex A records: 185.199.108.153, 185.199.109.153, 185.199.110.153, 185.199.111.153
    - Contains `pbau3r-sfdy.github.io` as the CNAME target
    - Contains letsencrypt CAA check note
    - Contains SSL provisioning wait warning (do not enable HTTPS redirect until certificate is active)
    - Contains default Squarespace record deletion warning
    - Contains `/wm-preflight <slug>` invocation in Step 2
    - Contains stage >= 5 validation in Step 1 with clear error message
    - Contains failure path (Step 6) that surfaces `gh run view --log-failed`
  </acceptance_criteria>

  <done>.claude/skills/wm-publish.md exists with all 6 steps, DNS guide with correct IP addresses and Squarespace-specific warnings, and preflight integration</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: E2E Verification — Full Deploy Pipeline</name>

  <what-built>
    The complete production deploy pipeline:
    - _scripts/build-single.mjs (single-site build wrapper)
    - .github/workflows/publish.yml (validate → build → CNAME → robots.txt → gh-pages push → wiring commit-back)
    - .claude/skills/wm-publish.md (operator skill with preflight + DNS guide)
    - CLAUDE.md + AGENTS.md (placeholder resolved, docs accurate)
  </what-built>

  <how-to-verify>
    **Pre-verification checks (run these first):**

    1. Confirm build-single.mjs works locally:
       ```bash
       node _scripts/build-single.mjs 2>&1   # should print usage + exit 1
       node _scripts/build-single.mjs sfdy-alt-clean   # should build successfully
       ```

    2. Confirm publish.yml exists and is structurally valid:
       ```bash
       gh workflow list | grep publish   # should show "Publish to Production"
       ```

    3. Confirm placeholder is gone:
       ```bash
       grep -rn '\[websites-org\]' CLAUDE.md AGENTS.md   # should return nothing
       ```

    **Full E2E test (choose one):**

    Option A — Re-deploy sfdy-alt-clean (already stage 6, safe to re-deploy):
    ```bash
    # In Claude, run:
    /wm-publish sfdy-alt-clean
    # Expected: preflight runs, workflow triggers, watch streams progress,
    # DNS guide prints at end, wiring.json last_deploy updates to today
    ```

    Option B — Validation-only test (no actual deploy):
    ```bash
    # Test that publish.yml exits early on a sub-stage-5 site
    # Create a temp wiring.json with stage: 3 in any test dir and trigger:
    gh workflow run publish.yml --field slug=orbint   # orbint is template, check its stage first
    # Watch the run fail at validation step (not at build)
    gh run watch $(gh run list --workflow publish.yml --limit 1 --json databaseId -q '.[0].databaseId')
    ```

    **What to verify:**
    - [ ] build-single.mjs exits 1 on no args and on unknown slug
    - [ ] publish.yml appears in `gh workflow list`
    - [ ] CLAUDE.md has no `[websites-org]` anywhere
    - [ ] wm-publish.md skill file exists and contains DNS guide with 4 IP addresses
    - [ ] (If Option A): Live URL resolves at https://www.starflight-dynamics.com
    - [ ] (If Option A): wiring.json for sfdy-alt-clean shows last_deploy as today's date
    - [ ] (If Option B): Workflow exits non-zero at validation step, not at build step
  </how-to-verify>

  <resume-signal>
    Type "approved" if all pre-verification checks pass and either Option A or B confirms correct behavior.
    Or describe any failures observed — e.g. "build-single.mjs fails with import error" or "publish.yml missing from workflow list".
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude skill → gh CLI | /wm-publish passes slug to gh workflow run — operator-controlled string reaches GitHub API |
| gh CLI → GitHub Actions | workflow_dispatch event with slug field triggers publish.yml in the runner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Slug passed from skill to gh workflow run | accept | Slug is validated by publish.yml step 4 (^[a-z0-9-]+$) before any file path use; gh CLI handles shell escaping |
| T-02-02 | Information Disclosure | gh run watch streams full workflow log to terminal | accept | No secrets are logged; WM_PUBLISH_PAT is masked by GitHub in all log output |
| T-02-03 | Denial of Service | gh run watch blocks indefinitely if workflow hangs | accept | publish.yml has timeout-minutes: 30; workflow runner terminates automatically |
| T-02-SC | Tampering | npm/pip/cargo installs (none in this plan) | accept | No package installs in this plan; only skill file creation |
</threat_model>

<verification>
After both tasks complete, the following must be true:

1. `test -f .claude/skills/wm-publish.md && echo ok` → ok
2. `grep -c '185.199.108.153' .claude/skills/wm-publish.md` → 1
3. `grep -c 'gh workflow run publish.yml' .claude/skills/wm-publish.md` → 1
4. `grep -c 'gh run watch' .claude/skills/wm-publish.md` → 1
5. `grep -rn '\[websites-org\]' CLAUDE.md AGENTS.md` → no output
6. Human checkpoint approved (E2E verification passed)
</verification>

<success_criteria>
- `.claude/skills/wm-publish.md` exists with complete 6-step flow and inline DNS guide
- DNS guide contains all 4 GitHub Pages apex A records, CNAME target, CAA note, SSL wait warning, default-record-deletion warning
- Preflight integration blocks on FAIL items before triggering workflow
- Human verifier confirms pre-verification checks pass and E2E deploy test succeeds or validation-failure test shows correct early exit
- All Phase 1 DEPLOY requirements (DEPLOY-01 through DEPLOY-08) are implemented across Plans 01 and 02
</success_criteria>

<output>
Create `.planning/phases/01-production-deploy-pipeline/01-02-SUMMARY.md` when done
</output>
