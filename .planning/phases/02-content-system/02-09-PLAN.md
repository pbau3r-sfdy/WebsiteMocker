---
phase: 02-content-system
plan: 09
type: execute
wave: 5
depends_on: [02-06, 02-07, 02-08]
files_modified:
  - _core/.claude/skills/wm-add-news.md
  - _core/.claude/skills/wm-add-job.md
  - _core/.claude/skills/wm-add-announcement.md
  - _core/.claude/skills/wm-add-blog.md
autonomous: true
requirements: [CONTENT-09, CONTENT-10]

must_haves:
  truths:
    - "/wm-add-news skill uses quoted date format (date: \"YYYY-MM-DD\") compatible with z.coerce.date() and GitHub web UI"
    - "/wm-add-job skill creates YYYY-MM-DD-slug.md in src/content/jobs/ with correct frontmatter fields"
    - "/wm-add-announcement skill creates YYYY-MM-DD-slug.md in src/content/announcements/"
    - "/wm-add-blog skill creates YYYY-MM-DD-slug.md in src/content/blog/"
    - "All four skills commit the content file with a conventional commit message"
  artifacts:
    - path: "_core/.claude/skills/wm-add-news.md"
      provides: "updated news content skill with quoted dates"
    - path: "_core/.claude/skills/wm-add-job.md"
      provides: "new job listing skill"
    - path: "_core/.claude/skills/wm-add-announcement.md"
      provides: "new announcement skill"
    - path: "_core/.claude/skills/wm-add-blog.md"
      provides: "new blog post skill"
  key_links:
    - from: "_core/.claude/skills/wm-add-job.md"
      to: "sites/<slug>/src/content/jobs/"
      via: "write YYYY-MM-DD-slug.md"
      pattern: "YYYY-MM-DD"
    - from: "_core/.claude/skills/wm-add-news.md"
      to: "sites/<slug>/src/content/news/"
      via: "write YYYY-MM-DD-slug.md with quoted date"
      pattern: "date: \"YYYY-MM-DD\""
---

## Phase Goal

**As an** operator, **I want to** add content to any active site using a single skill command and have it committed with the correct schema, **so that** non-technical contributors can publish content via the GitHub web UI without a local build step.

<objective>
Update the existing /wm-add-news skill and create three new content skills (/wm-add-job, /wm-add-announcement, /wm-add-blog). All skills write YYYY-MM-DD-slug.md files with quoted date strings and commit them in one step.

Purpose: Completes CONTENT-09 and CONTENT-10. Skills are the operator interface to the content system — they enforce naming conventions and field completeness that the schema validates at build time.

Output: 4 skill files in _core/.claude/skills/.
</objective>

<execution_context>
@/Users/pbau3r/DevWorks/Websites/WebsiteMocker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pbau3r/DevWorks/Websites/WebsiteMocker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pbau3r/DevWorks/Websites/WebsiteMocker/.planning/ROADMAP.md
@/Users/pbau3r/DevWorks/Websites/WebsiteMocker/.planning/phases/02-content-system/02-RESEARCH.md

<interfaces>
<!-- Canonical frontmatter per collection type — from RESEARCH.md §Code Examples: -->

<!-- news (CONTENT-05 fields + short?): -->
<!-- title: "..." | date: "YYYY-MM-DD" | summary: "..." | image: "/images/news/..." | imageCredit: "..." | tags: ["..."] | short: "..." (optional) -->

<!-- jobs (CONTENT-06 fields): -->
<!-- title: "..." | department: "..." | location: "..." | type: "full-time" | open: true | date: "YYYY-MM-DD" -->

<!-- announcements (CONTENT-07 fields): -->
<!-- title: "..." | date: "YYYY-MM-DD" | summary: "..." | tags: ["..."] -->

<!-- blog (CONTENT-08 fields): -->
<!-- title: "..." | date: "YYYY-MM-DD" | author: "..." | summary: "..." | image: "/images/blog/..." | tags: ["..."] -->

<!-- KEY: dates MUST be quoted strings — "YYYY-MM-DD" not YYYY-MM-DD -->
<!-- z.coerce.date() accepts both, but quoted form works in GitHub web UI without Zod -->
<!-- CONTENT-10: files MUST be named YYYY-MM-DD-slug.md -->

<!-- Existing wm-add-news.md skill (to update): -->
<!-- - Step 4 frontmatter: uses unquoted date: YYYY-MM-DD — must change to date: "YYYY-MM-DD" -->
<!-- - Step 5: npm run build verify step (keep or remove per judgement — heavy for content-only) -->
<!-- - Note at bottom: "Date format MUST be YYYY-MM-DD (no quotes unless using Zod date)" — REVERSE this note -->
<!-- - Missing: tags field is not in current skill — add it -->

<!-- Commit message format (follow existing wm-add-news pattern): -->
<!-- git commit -m "content(<slug>): add <type> — <title>" -->
<!-- where <slug> is the site slug (e.g. sfdy-alt-clean, mogwai-systems, parrot-capital) -->

<!-- Skills live in _core/.claude/skills/ and are inherited by all sites -->
<!-- The executor is Claude; these are markdown prompt files (not executable scripts) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update /wm-add-news + create /wm-add-job</name>
  <files>_core/.claude/skills/wm-add-news.md, _core/.claude/skills/wm-add-job.md</files>
  <read_first>
    - _core/.claude/skills/wm-add-news.md — read full file (must see current date format note and steps before updating)
    - .planning/phases/02-content-system/02-RESEARCH.md §Skill File Pattern — wm-add-job example structure
    - .planning/phases/02-content-system/02-RESEARCH.md §Architecture Patterns — Pattern 5 (quoted date frontmatter format)
  </read_first>
  <action>
    Update _core/.claude/skills/wm-add-news.md. Keep the overall structure but make these targeted changes:
    - Step 4 frontmatter template: change `date: YYYY-MM-DD` to `date: "YYYY-MM-DD"` (add quotes)
    - Step 4: add `tags: ["tag1", "tag2"]` field to frontmatter template (optional, can be empty list)
    - Notes section: change "Date format MUST be YYYY-MM-DD (no quotes unless using Zod date)" to "Date MUST be a quoted string: date: \"YYYY-MM-DD\" — both unquoted and quoted dates work at build time, but quoted dates work when contributors edit via GitHub web UI"
    - Remove or make optional the "Verify the build passes: npm run build" step — skills that run a full site build are slow; instead note "Build runs automatically on next deploy"
    - Keep all other steps (gather, slug, images, social draft, commit, report) unchanged

    Create _core/.claude/skills/wm-add-job.md. Follow the exact structure from RESEARCH.md §Skill File Pattern:
    Title: /wm-add-job. Add a job listing to this site.
    Steps:
    1. Gather: title, department (optional), location (e.g. "Berlin, Germany" or "Remote"), type (full-time | part-time | contract), open (true | false, default true), date (YYYY-MM-DD; default today), body text (full job description in Markdown)
    2. Generate slug: YYYY-MM-DD-title-words (lowercase, hyphens, max ~40 chars)
    3. Write to src/content/jobs/<slug>.md with frontmatter: title (quoted), department (quoted, omit if not provided), location (quoted), type (quoted), open (boolean — no quotes), date (quoted "YYYY-MM-DD"). Body text below the frontmatter.
    4. Commit and push: `git add src/content/jobs/<slug>.md && git commit -m "content(<site-slug>): add job — <title>" && git push`
    5. Report: confirm file path and remind operator that the site rebuilds on next deploy or /wm-publish run.
    Notes: All fields except department and body are required. Closed jobs (open: false) are excluded from the default /jobs list but still buildable.
  </action>
  <verify>
    <automated>
      grep '"YYYY-MM-DD"' /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/.claude/skills/wm-add-news.md
      test -f /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/.claude/skills/wm-add-job.md && echo "wm-add-job OK"
      grep "src/content/jobs" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/.claude/skills/wm-add-job.md
      grep "full-time\|part-time\|contract" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/.claude/skills/wm-add-job.md
    </automated>
  </verify>
  <done>wm-add-news.md uses quoted date format. wm-add-job.md exists with gather → slug → write → commit steps targeting src/content/jobs/. Both files are operator-ready skill prompts.</done>
</task>

<task type="auto">
  <name>Task 2: Create /wm-add-announcement + /wm-add-blog</name>
  <files>_core/.claude/skills/wm-add-announcement.md, _core/.claude/skills/wm-add-blog.md</files>
  <read_first>
    - _core/.claude/skills/wm-add-job.md — reference for skill file structure (just created in Task 1)
    - .planning/phases/02-content-system/02-RESEARCH.md §Code Examples — announcement and blog frontmatter templates
  </read_first>
  <action>
    Create _core/.claude/skills/wm-add-announcement.md. Structure mirrors wm-add-job.md. Title: /wm-add-announcement. Add an announcement to this site.
    Steps:
    1. Gather: title, date (YYYY-MM-DD; default today), summary (one sentence), tags (optional list), body text (Markdown).
    2. Generate slug: YYYY-MM-DD-title-words.
    3. Write to src/content/announcements/<slug>.md with frontmatter: title (quoted), date ("YYYY-MM-DD"), summary (quoted), tags (list of quoted strings, omit if empty). Body text below.
    4. Commit: `git add src/content/announcements/<slug>.md && git commit -m "content(<site-slug>): add announcement — <title>" && git push`
    5. Report file path and remind about next deploy.

    Create _core/.claude/skills/wm-add-blog.md. Title: /wm-add-blog. Add a blog post to this site.
    Steps:
    1. Gather: title, date (YYYY-MM-DD; default today), author (optional), summary (one sentence), image path (optional — local file, will be copied to public/images/blog/), tags (optional list), body text (Markdown).
    2. Generate slug: YYYY-MM-DD-title-words.
    3. Copy any images: `cp <source> public/images/blog/<filename>` (rename to YYYY-MM-DD-keyword.jpg).
    4. Write to src/content/blog/<slug>.md with frontmatter: title (quoted), date ("YYYY-MM-DD"), author (quoted, omit if not provided), summary (quoted), image ("/images/blog/<filename>", omit if none), tags (list, omit if empty). Body text below.
    5. Commit: `git add src/content/blog/<slug>.md public/images/blog/ && git commit -m "content(<site-slug>): add blog post — <title>" && git push`
    6. Report file path.
    Notes: Images are optional. Date must be quoted string. Tags are optional but improve discoverability.
  </action>
  <verify>
    <automated>
      test -f /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/.claude/skills/wm-add-announcement.md && echo "announcement skill OK"
      test -f /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/.claude/skills/wm-add-blog.md && echo "blog skill OK"
      grep "src/content/announcements" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/.claude/skills/wm-add-announcement.md
      grep "src/content/blog" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/.claude/skills/wm-add-blog.md
      grep '"YYYY-MM-DD"' /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/.claude/skills/wm-add-announcement.md
      grep '"YYYY-MM-DD"' /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/.claude/skills/wm-add-blog.md
    </automated>
  </verify>
  <done>Both skill files exist. wm-add-announcement.md writes to src/content/announcements/. wm-add-blog.md writes to src/content/blog/. Both use quoted date format. Both include git commit step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → content files | Skills guide Claude to write and commit .md files to the site repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | skill-generated content files | accept | Content commits go through git; operator reviews before push; no automated trust escalation |
| T-02-SC | Tampering | npm installs | accept | No new installs; skill files are markdown prompt documents |
</threat_model>

<verification>
- All 4 skill files exist in _core/.claude/skills/
- All skill files reference `"YYYY-MM-DD"` quoted date format (grep check)
- Each skill targets the correct src/content/<type>/ directory
- Each skill includes git add + commit + push step
- wm-add-news.md date note updated to recommend quoted strings
</verification>

<success_criteria>
`grep '"YYYY-MM-DD"' _core/.claude/skills/wm-add-news.md _core/.claude/skills/wm-add-job.md _core/.claude/skills/wm-add-announcement.md _core/.claude/skills/wm-add-blog.md` returns 4 matches (one per file). All 4 skill files contain commit message step.
</success_criteria>

<output>
Create `.planning/phases/02-content-system/02-09-SUMMARY.md` when done.
</output>
