---
phase: 02-content-system
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - _core/src/components/AnnouncementCard.astro
  - _core/src/pages/announcements/index.astro
  - _core/src/pages/announcements/[slug].astro
  - _core/src/components/BlogCard.astro
  - _core/src/pages/blog/index.astro
  - _core/src/pages/blog/[slug].astro
autonomous: true
requirements: [CONTENT-07, CONTENT-08]

must_haves:
  truths:
    - "AnnouncementCard renders a single flex-column row with date, title, summary, TagPill chips"
    - "announcements/index.astro uses eyebrow ANNOUNCEMENTS and h1 Announcements"
    - "BlogCard renders same grid-card visual treatment as NewsCard with addition of author line"
    - "blog/index.astro uses CSS grid (auto-fill minmax(280px, 1fr)) not row layout"
    - "All four new pages wrap dates in <time datetime='YYYY-MM-DD'> element"
    - "Back links: '← All announcements' on announcement detail, '← All posts' on blog detail"
  artifacts:
    - path: "_core/src/components/AnnouncementCard.astro"
      provides: "announcement list row with TagPill chips"
    - path: "_core/src/pages/announcements/index.astro"
      provides: "announcements list page"
    - path: "_core/src/pages/announcements/[slug].astro"
      provides: "announcement detail page"
    - path: "_core/src/components/BlogCard.astro"
      provides: "blog grid card with author line"
    - path: "_core/src/pages/blog/index.astro"
      provides: "blog list page (grid layout)"
    - path: "_core/src/pages/blog/[slug].astro"
      provides: "blog detail page"
  key_links:
    - from: "_core/src/components/AnnouncementCard.astro"
      to: "_core/src/components/TagPill.astro"
      via: "import TagPill"
      pattern: "TagPill"
    - from: "_core/src/pages/blog/index.astro"
      to: "astro:content"
      via: "getCollection('blog')"
      pattern: "getCollection"
---

## Phase Goal

**As an** operator, **I want to** add content to any active site using a single skill command and have it committed with the correct schema, **so that** non-technical contributors can publish content via the GitHub web UI without a local build step.

<objective>
Create announcement and blog page templates (list + detail) and their card components for the _core template.

Purpose: Completes the four-collection template set in _core. Runs in parallel with Plan 02 (no file conflicts).

Output: AnnouncementCard, announcements list+detail pages, BlogCard, blog list+detail pages — all in _core/src/.
</objective>

<execution_context>
@/Users/pbau3r/DevWorks/Websites/WebsiteMocker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pbau3r/DevWorks/Websites/WebsiteMocker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pbau3r/DevWorks/Websites/WebsiteMocker/.planning/ROADMAP.md
@/Users/pbau3r/DevWorks/Websites/WebsiteMocker/.planning/phases/02-content-system/02-RESEARCH.md
@/Users/pbau3r/DevWorks/Websites/WebsiteMocker/.planning/phases/02-content-system/02-UI-SPEC.md

<interfaces>
<!-- From 02-01-SUMMARY.md — schemas: -->
<!-- announcementsSchema fields: title, date (coerce), summary, tags? -->
<!-- blogSchema fields: title, date (coerce), author?, summary, image?, tags? -->

<!-- TagPill.astro: import TagPill from '../../components/TagPill.astro'; <TagPill tag={t} /> -->

<!-- AnnouncementCard UI-SPEC anatomy (§Component Inventory): -->
<!-- flex-direction column; gap 12px (NOT 16px — spacing exception) -->
<!-- row hover: background rgba(255,255,255,.04) with border-bottom transition to --border-accent -->
<!-- NO thumbnail column — single full-width layout -->

<!-- BlogCard UI-SPEC anatomy (§Component Inventory): -->
<!-- Identical visual treatment to NewsCard grid card: -->
<!-- display flex; flex-direction column; background --surface-1; border 1px solid --border-subtle -->
<!-- hover: border-color --border-accent; transform translateY(-2px); transition 200ms cubic-bezier(.2,.6,.2,1) -->
<!-- Additional: author line — 11px; weight 300; --text-muted; --font-mono; no uppercase -->

<!-- Blog list layout (§Page Layouts): grid auto-fill minmax(280px, 1fr); gap 24px -->
<!-- NOT row layout — blog uses the same card grid as news on the _core index page -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AnnouncementCard + announcements list and detail pages</name>
  <files>_core/src/components/AnnouncementCard.astro, _core/src/pages/announcements/index.astro, _core/src/pages/announcements/[slug].astro</files>
  <read_first>
    - .planning/phases/02-content-system/02-UI-SPEC.md §Component Inventory — AnnouncementCard anatomy (flex-direction column; gap 12px; row hover pattern)
    - .planning/phases/02-content-system/02-UI-SPEC.md §Page Layouts — list page structure (eyebrow, h1, .list wrapper)
    - .planning/phases/02-content-system/02-UI-SPEC.md §Copywriting Contract — "ANNOUNCEMENTS", "Announcements", "← All announcements", "No announcements yet"
    - .planning/phases/02-content-system/02-UI-SPEC.md §Accessibility Requirements — <time> element contract
    - _core/src/pages/news/index.astro — reference for header structure pattern
  </read_first>
  <action>
    Create _core/src/components/AnnouncementCard.astro. Props: id (string), title (string), date (Date), summary (string), tags (string[] | undefined). Import TagPill from './TagPill.astro'. Render `<a href="/announcements/{id}" class="row">` containing div.info with: `<time class="mono date" datetime={date.toISOString().split('T')[0]}>{stamp}</time>`, `<h2>{title}</h2>`, `<p>{summary}</p>`, and if tags?.length then `<div class="tags">{tags.map(t => <TagPill tag={t} />)}</div>`. CSS: .row display flex; flex-direction column; gap 12px (UI-SPEC spacing exception — not 16px); padding 36px 0; border-bottom 1px solid var(--border-subtle); transition border-bottom-color 200ms cubic-bezier(.2,.6,.2,1); .row:hover border-bottom-color var(--border-accent); background rgba(255,255,255,.04); padding-left transition 8px. .date color var(--accent). h2 font-size clamp(1.0625rem, 2vw, 1.5rem); font-weight 700; line-height 1.24. p font-size 0.9375rem; font-weight 300; line-height 1.65; color var(--text-soft). .tags display flex; flex-wrap wrap; gap 8px.

    Create _core/src/pages/announcements/index.astro. Import getCollection, Layout, Nav, Footer, AnnouncementCard. Get all announcements sorted by date descending. Layout title "Announcements". Header eyebrow ANNOUNCEMENTS, h1 Announcements. List .list div (max-width 1000px; margin 0 auto; padding 24px var(--gutter) 120px). Render AnnouncementCard per entry passing id={entry.id} and entry.data fields. Empty state: `<p class="empty">No announcements yet.</p>`.

    Create _core/src/pages/announcements/[slug].astro. Import getCollection and render from 'astro:content', Layout, Nav, Footer, TagPill. getStaticPaths maps entries to params { slug: entry.id }. Article structure: back link `← All announcements` → /announcements. Header: `<time>` with date, h1 title, p.summary, tags div with TagPill chips (if tags present). Body: `<div class="body"><Content /></div>`. CSS matches news detail (main 72px/120px padding; article max-width 760px; .back margin-bottom 44px).
  </action>
  <verify>
    <automated>
      test -f /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/src/components/AnnouncementCard.astro
      test -f /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/src/pages/announcements/index.astro
      test -f "/Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/src/pages/announcements/[slug].astro"
      grep "gap: 12px" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/src/components/AnnouncementCard.astro
      grep "TagPill" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/src/components/AnnouncementCard.astro
      grep "← All announcements" "/Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/src/pages/announcements/[slug].astro"
    </automated>
  </verify>
  <done>AnnouncementCard renders flex-column row with 12px gap and TagPill chips. announcements/index.astro has ANNOUNCEMENTS eyebrow and empty state. announcements/[slug].astro uses entry.id and standalone render().</done>
</task>

<task type="auto">
  <name>Task 2: Create BlogCard + blog list and detail pages</name>
  <files>_core/src/components/BlogCard.astro, _core/src/pages/blog/index.astro, _core/src/pages/blog/[slug].astro</files>
  <read_first>
    - .planning/phases/02-content-system/02-UI-SPEC.md §Component Inventory — BlogCard anatomy (identical to NewsCard + author line at 11px mono --text-muted)
    - .planning/phases/02-content-system/02-UI-SPEC.md §Page Layouts — blog list uses grid layout (auto-fill minmax(280px, 1fr); gap 24px; padding 24px var(--gutter) 120px)
    - .planning/phases/02-content-system/02-UI-SPEC.md §Copywriting Contract — "BLOG", "Blog", "← All posts", "No posts yet"
    - _core/src/components/NewsCard.astro — copy this visual treatment exactly, then add author line
  </read_first>
  <action>
    Create _core/src/components/BlogCard.astro. Props: id (string), title (string), date (Date), author (string | undefined), image (string | undefined). Render `<a href="/blog/{id}" class="card">`. Structure: div.thumb containing `<img src={image} alt="" loading="lazy" />` if image present, else `<div class="placeholder"></div>`. div.body containing `<time class="mono date" datetime={date.toISOString().split('T')[0]}>{stamp}</time>`, `<h3>{title}</h3>`, and if author then `<p class="author">{author}</p>`. CSS: identical to NewsCard (.card display flex; flex-direction column; background var(--surface-1); border 1px solid var(--border-subtle); overflow hidden; transition border-color + transform 200ms cubic-bezier(.2,.6,.2,1)); .card:hover border-color var(--border-accent); transform translateY(-2px). .thumb aspect-ratio 16/10; overflow hidden; background var(--surface-2). .body flex-direction column; gap 6px; padding 14px 16px 16px. .date color var(--accent). h3 font-size 0.9375rem; font-weight 700; line-height 1.3; color var(--text-primary). .author font-size 11px; font-weight 300; color var(--text-muted); font-family var(--font-mono); no text-transform.

    Create _core/src/pages/blog/index.astro. Import getCollection, Layout, Nav, Footer, BlogCard. Get all blog entries sorted by date descending. Layout title "Blog". Header eyebrow BLOG, h1 Blog. GRID layout (not row): `.grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 24px; padding: 24px var(--gutter) 120px; }`. Render BlogCard per entry. Empty state: `<p class="empty">No posts yet.</p>`.

    Create _core/src/pages/blog/[slug].astro. Import getCollection and render from 'astro:content', Layout, Nav, Footer, TagPill. getStaticPaths maps entries to params { slug: entry.id }. Article: back link `← All posts` → /blog. Header: `<time>` with date, if author then `<p class="byline">By {post.data.author}</p>` (11px mono), h1 title, p.summary. If image: `<img class="lead" src={image} alt="" />`. Tags div with TagPill chips if tags present. Body: `<div class="body"><Content /></div>`. CSS matches news detail pattern.
  </action>
  <verify>
    <automated>
      test -f /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/src/components/BlogCard.astro
      test -f /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/src/pages/blog/index.astro
      test -f "/Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/src/pages/blog/[slug].astro"
      grep "auto-fill" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/src/pages/blog/index.astro
      grep "← All posts" "/Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/src/pages/blog/[slug].astro"
      grep "translateY" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/src/components/BlogCard.astro
      grep "var(--text-muted)" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/src/components/BlogCard.astro
    </automated>
  </verify>
  <done>BlogCard renders NewsCard-style grid card with author line in --text-muted mono font. blog/index.astro uses grid layout (not rows). blog/[slug].astro uses entry.id and standalone render(). Back link says "← All posts".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown content → rendered HTML | Blog and announcement bodies rendered at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | _core announcement/blog templates | accept | Static build-time rendering; no runtime injection surface |
| T-02-SC | Tampering | npm installs | accept | No new installs; astro:content is built-in |
</threat_model>

<verification>
- AnnouncementCard uses gap 12px (not 16px) — spacing exception per UI-SPEC
- BlogCard hover applies translateY(-2px) — grid card pattern matches NewsCard
- blog/index.astro uses grid layout with repeat(auto-fill, minmax(280px, 1fr))
- All detail pages use standalone render() and entry.id for params
- Back links: "← All announcements" and "← All posts" exactly
</verification>

<success_criteria>
All 6 files exist in _core/src/. `grep "auto-fill" _core/src/pages/blog/index.astro` returns a match. `grep "gap: 12px" _core/src/components/AnnouncementCard.astro` returns a match. No entry.slug or entry.render() references in any new file.
</success_criteria>

<output>
Create `.planning/phases/02-content-system/02-03-SUMMARY.md` when done.
</output>
