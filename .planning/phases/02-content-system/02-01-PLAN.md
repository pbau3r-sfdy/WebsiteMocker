---
phase: 02-content-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _core/src/content.config.ts
  - _core/src/content/config.ts
  - _core/src/components/TagPill.astro
  - _core/src/components/TypeBadge.astro
autonomous: true
requirements: [CONTENT-02, CONTENT-03, CONTENT-10]

must_haves:
  truths:
    - "_core/src/content.config.ts exists and exports newsSchema, jobsSchema, announcementsSchema, blogSchema using z.coerce.date()"
    - "_core/src/content/config.ts has been deleted — Astro 4 legacy config no longer present in _core"
    - "_core/src/components/TagPill.astro renders a <span class='tag'> using only CSS custom property tokens"
    - "_core/src/components/TypeBadge.astro maps type values (full-time/part-time/contract) to uppercase display labels"
  artifacts:
    - path: "_core/src/content.config.ts"
      provides: "canonical Zod schemas — imported by all per-site content.config.ts files"
      exports: ["newsSchema", "jobsSchema", "announcementsSchema", "blogSchema"]
    - path: "_core/src/components/TagPill.astro"
      provides: "reusable tag pill UI primitive"
    - path: "_core/src/components/TypeBadge.astro"
      provides: "job type badge UI primitive"
  key_links:
    - from: "sites/*/src/content.config.ts"
      to: "_core/src/content.config.ts"
      via: "TypeScript relative import"
      pattern: "from '../../_core/src/content.config.ts'"
---

## Phase Goal

**As an** operator, **I want to** add content to any active site using a single skill command and have it committed with the correct schema, **so that** non-technical contributors can publish content via the GitHub web UI without a local build step.

<objective>
Create the canonical Zod schema library that all sites will import, and two shared UI primitive components used by content card templates.

Purpose: All per-site content.config.ts files import from _core rather than defining their own schemas — preventing drift. z.coerce.date() ensures GitHub web UI date strings don't break CI.

Output: _core/src/content.config.ts (4 Zod schemas), TagPill.astro, TypeBadge.astro; _core/src/content/config.ts deleted.
</objective>

<execution_context>
@/Users/pbau3r/DevWorks/Websites/WebsiteMocker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pbau3r/DevWorks/Websites/WebsiteMocker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pbau3r/DevWorks/Websites/WebsiteMocker/.planning/ROADMAP.md
@/Users/pbau3r/DevWorks/Websites/WebsiteMocker/.planning/phases/02-content-system/02-RESEARCH.md
@/Users/pbau3r/DevWorks/Websites/WebsiteMocker/.planning/phases/02-content-system/02-UI-SPEC.md

<interfaces>
<!-- Existing file being replaced -->
<!-- _core/src/content/config.ts (Astro 4 — to delete): -->
<!-- import { defineCollection, z } from 'astro:content'; -->
<!-- const news = defineCollection({ type: 'content', schema: z.object({ title, date: z.date(), summary, image?, imageCredit? }) }); -->
<!-- export const collections = { news }; -->

<!-- New _core/src/content.config.ts exports ONLY schema objects — no defineCollection, no glob(), no collections export -->
<!-- Each site's src/content.config.ts does the defineCollection + glob() wiring -->

<!-- newsSchema must include short?: z.string().optional() because sites/sfdy-alt-clean/src/pages/index.astro -->
<!-- reads post.data.short ?? post.data.title for news card titles. Dropping it breaks the homepage. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create _core canonical schema library + delete Astro 4 config</name>
  <files>_core/src/content.config.ts, _core/src/content/config.ts (delete)</files>
  <read_first>
    - _core/src/content/config.ts — read to confirm current Astro 4 schema before deleting
    - .planning/phases/02-content-system/02-RESEARCH.md §Pattern 1 — exact schema structure to create
    - .planning/phases/02-content-system/02-RESEARCH.md §Common Pitfalls — Pitfall 1 (must delete old file in same commit)
  </read_first>
  <action>
    Create _core/src/content.config.ts. Import z from 'astro:content'. Export four named schema constants:

    newsSchema: z.object with fields title (z.string()), date (z.coerce.date()), summary (z.string()), image (z.string().optional()), imageCredit (z.string().optional()), tags (z.array(z.string()).optional()), short (z.string().optional()). The short field is required for sfdy-alt-clean homepage compatibility (sites/sfdy-alt-clean/src/pages/index.astro reads post.data.short ?? post.data.title) even though it is not in the REQUIREMENTS — treat it as an additive optional field.

    jobsSchema: z.object with fields title (z.string()), department (z.string().optional()), location (z.string()), type (z.enum(['full-time', 'part-time', 'contract'])), open (z.boolean().default(true)), date (z.coerce.date()).

    announcementsSchema: z.object with fields title (z.string()), date (z.coerce.date()), summary (z.string()), tags (z.array(z.string()).optional()).

    blogSchema: z.object with fields title (z.string()), date (z.coerce.date()), author (z.string().optional()), summary (z.string()), image (z.string().optional()), tags (z.array(z.string()).optional()).

    No defineCollection calls. No glob() loader. No collections export. This file is a schema library only.

    After creating _core/src/content.config.ts, delete _core/src/content/config.ts. Both operations go in the same git commit per RESEARCH.md Pitfall 1.
  </action>
  <verify>
    <automated>
      grep -c "z.coerce.date()" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/src/content.config.ts
      # must return 4 (one per schema)
      test ! -f /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/src/content/config.ts && echo "OLD FILE DELETED OK"
      grep "export const newsSchema\|export const jobsSchema\|export const announcementsSchema\|export const blogSchema" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/src/content.config.ts
      # must return all 4 lines
    </automated>
  </verify>
  <done>_core/src/content.config.ts exists exporting newsSchema, jobsSchema, announcementsSchema, blogSchema — all using z.coerce.date(). _core/src/content/config.ts does not exist.</done>
</task>

<task type="auto">
  <name>Task 2: Create TagPill and TypeBadge shared UI components</name>
  <files>_core/src/components/TagPill.astro, _core/src/components/TypeBadge.astro</files>
  <read_first>
    - .planning/phases/02-content-system/02-UI-SPEC.md §Component Inventory — exact anatomy, padding, border, font spec for both components
    - _core/src/components/NewsCard.astro — reference for Astro component file structure and scoped CSS pattern to follow
  </read_first>
  <action>
    Create _core/src/components/TagPill.astro. Props: tag (string). Render a single `<span class="tag">{tag}</span>`. Scoped CSS: display inline-block; padding 4px 8px; border 1px solid var(--border-subtle); font-size 11px; font-weight 300; font-family var(--font-mono); text-transform uppercase; letter-spacing 0.12em; color var(--accent); background transparent. No hover state.

    Create _core/src/components/TypeBadge.astro. Props: type (string, one of 'full-time' | 'part-time' | 'contract'). Map to display label using a const record: { 'full-time': 'FULL-TIME', 'part-time': 'PART-TIME', 'contract': 'CONTRACT' }. Render a `<span class="badge">{label}</span>`. Scoped CSS: same as TagPill except border uses var(--border-strong) (not var(--border-subtle)). No hover state. All CSS values reference CSS custom property tokens only — no hardcoded hex values.
  </action>
  <verify>
    <automated>
      grep "var(--border-subtle)" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/src/components/TagPill.astro
      grep "var(--border-strong)" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/src/components/TypeBadge.astro
      grep "var(--accent)" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/src/components/TagPill.astro
      grep "full-time\|part-time\|contract" /Users/pbau3r/DevWorks/Websites/WebsiteMocker/_core/src/components/TypeBadge.astro
    </automated>
  </verify>
  <done>TagPill.astro renders tag pills with token-only CSS. TypeBadge.astro maps type enum values to uppercase labels with --border-strong border. Both files exist in _core/src/components/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → git | Schema file changes committed locally then pushed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | _core/src/content.config.ts | accept | Schema-only file; no runtime execution; changes tracked in git |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new npm installs in this phase; all Content Layer API features built into astro@5.18.2 already installed |
</threat_model>

<verification>
- _core/src/content.config.ts exists and exports exactly 4 schema constants
- All date fields use z.coerce.date() — verified with grep -c returning 4
- _core/src/content/config.ts does not exist — Astro 4 legacy mode disabled in _core
- TagPill.astro and TypeBadge.astro exist with token-only CSS (no hardcoded hex)
</verification>

<success_criteria>
`grep -c "z.coerce.date()" _core/src/content.config.ts` returns 4. `test ! -f _core/src/content/config.ts` exits 0. Both component files exist in _core/src/components/.
</success_criteria>

<output>
Create `.planning/phases/02-content-system/02-01-SUMMARY.md` when done.
</output>
