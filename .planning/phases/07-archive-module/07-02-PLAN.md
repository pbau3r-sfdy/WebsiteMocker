---
phase: 07-archive-module
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - .claude/skills/wm-archive-browse.md
autonomous: true
requirements:
  - ARCH-05

must_haves:
  truths:
    - "Operator runs `/wm-archive-browse sfdy-alt-clean` in Claude and receives a year-grouped timeline with if_ inspection URLs without any manual script invocation"
    - "Operator selects a timestamp; Claude prints the full if_ URL and instructs them to open it in their browser (D-12)"
    - "Claude asks 'Capture this snapshot? (y/n)' and does NOT proceed to capture until the operator explicitly types y (D-12, D-13)"
    - "If operator types y, Claude runs `node _scripts/archive-browse.mjs <slug> --capture <timestamp>` and reports the _captures/<slug>-<timestamp>/ output path (D-13)"
    - "If operator types n, Claude reports 'Timeline browsing complete' without running any capture subprocess"
  artifacts:
    - path: ".claude/skills/wm-archive-browse.md"
      provides: "Interactive guided skill: browse → inspect → optional capture"
      contains: "/wm-archive-browse"
  key_links:
    - from: ".claude/skills/wm-archive-browse.md (Step 2)"
      to: "_scripts/archive-browse.mjs"
      via: "node subprocess in bash code block"
      pattern: "archive-browse.mjs"
    - from: ".claude/skills/wm-archive-browse.md (Step 5)"
      to: "_scripts/archive-browse.mjs --capture"
      via: "node subprocess in bash code block"
      pattern: "archive-browse.mjs.*--capture"
---

<objective>
Create `.claude/skills/wm-archive-browse.md` — the guided Claude skill that wraps `archive-browse.mjs`. The skill walks the operator through: collect slug input → run timeline → display results → inspect a snapshot by printing its if_ URL → confirm capture gate → optionally run --capture. Follows the shell-out → display → confirm → exec pattern established by `wm-gen-docs.md` and `wm-ingest.md`.

Purpose: Give operators an interactive, guided entry point so they never need to remember archive-browse.mjs flag syntax.
Output: `.claude/skills/wm-archive-browse.md` (single new file, Markdown, no code changes).
</objective>

<execution_context>
@AGENTS.md
</execution_context>

<context>
@.planning/phases/07-archive-module/07-CONTEXT.md
@.planning/phases/07-archive-module/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .claude/skills/wm-archive-browse.md (ARCH-05)</name>
  <files>.claude/skills/wm-archive-browse.md</files>

  <read_first>
    - `.claude/skills/wm-gen-docs.md` — read in full. This is the primary analog: copy the opening format (`# /wm-skill-name` → one-paragraph description → `---` → `## Steps` → `### N. Step name`), the confirm gate language in Step 5 ("Do NOT proceed to Step 6 until the operator explicitly types `y`"), and the Notes section format (`- **\`key concept\`** — one-sentence explanation`).
    - `.claude/skills/wm-ingest.md` — read Steps 1–4 and the Notes section. Confirms the shell-out → display → confirm → exec pattern. The Step 3 (Analyze: run script, read stdout, present to operator) and Step 4 (Confirm gate) sequence maps directly to this skill's Steps 2–4.
    - `.planning/phases/07-archive-module/07-CONTEXT.md` decisions D-11, D-12, D-13 — the exact interaction model for this skill. Each decision maps to a step.
  </read_first>

  <action>
    Create `.claude/skills/wm-archive-browse.md`. The file must follow the exact structural pattern of `wm-gen-docs.md` and `wm-ingest.md` as documented in PATTERNS.md Pattern 7.

    Opening: `# /wm-archive-browse` heading, then one paragraph (no subheading) describing what the skill does: browse the Wayback Machine snapshot history for any site in `wiring.json` and optionally capture a historical snapshot's design DNA. Note that it requires no API keys. Then `---` separator, then `## Steps`.

    The skill has six numbered steps (`### 1.` through `### 6.`). Map each decision to a step:

    Step 1 — "Collect inputs": Ask the operator for a slug (must exist in `sites/`) or a bare domain name. Validate the slug is non-empty before proceeding. Mention that `--limit N` can optionally be appended to override the default 100-snapshot fetch.

    Step 2 — "Run timeline" (D-11): Execute the script and display its output. Use a fenced `bash` code block: `node _scripts/archive-browse.mjs <slug>`. After the code block, one sentence: "Read all stdout and present the full year-grouped timeline to the operator." No reformatting of the output — reproduce it exactly as printed.

    Step 3 — "Select a snapshot" (D-11): Prompt the operator: "Enter the 14-digit timestamp you want to inspect (e.g. 20240315123045), or press Enter to exit." If the operator provides a timestamp, continue to Step 4. If they press Enter or type nothing, go to Step 6 (done).

    Step 4 — "Inspect snapshot" (D-12): Print the if_ URL in a plain code block (not bash — just an indented block or fenced without a language tag so it is easy to click): `https://web.archive.org/web/<timestamp>if_/<domain>`. Then say: "Open this URL in your browser to inspect the historical design." Then ask: "Capture this snapshot? (y/n)". Do NOT proceed to Step 5 until the operator explicitly types `y`. This confirm gate is mandatory — it cannot be bypassed, even if the operator has already said they want to capture.

    Step 5 — "Run capture" (D-13): Execute the capture handoff. Use a fenced `bash` code block: `node _scripts/archive-browse.mjs <slug> --capture <timestamp>`. After the code block: "The script validates the timestamp against the CDX response, then hands off to `capture-site.mjs`. Playwright will run inline — wait for it to complete." After the command finishes, report: "Design DNA written to `_captures/<slug>-<timestamp>/`."

    Step 6 — "Done": "Timeline browsing complete. Run `/wm-archive-browse` again to inspect another snapshot."

    After Step 6, add a `---` separator, then a `## Notes` section with the following bullets (each uses the `- **\`key concept\`** —` format from the analog skills):
    - `**\`--limit N\`** —` appending this flag to the Step 2 command overrides the default 100-snapshot fetch; useful for domains with a long history
    - `**domain is read from \`wiring.json\`** —` the script reads `sites/<slug>/wiring.json`; if `domain` is null or missing, the script exits 1 with an actionable error — update `wiring.json` before re-running
    - `**bare domain input** —` if the operator provides a domain name (contains a dot) instead of a slug, the script uses it directly without reading `wiring.json`; --capture is not available in domain mode (no slug to build the output path)
    - `**capture output is namespaced** —` historical captures land in `_captures/<slug>-<timestamp>/` to prevent overwriting the live site capture in `_captures/<slug>/`
    - `**CDX fetch requires internet** —` the script contacts `web.archive.org`; if the network is unavailable, the script exits 1 with a clear message

    Do NOT add a `## Prerequisites` section — the analog skills (wm-gen-docs.md, wm-ingest.md) do not have one. Do NOT add a `## Usage` section before `## Steps`. The skill opens directly with the description paragraph followed by `---` and `## Steps`.

    Command syntax in code blocks must use exactly `node _scripts/archive-browse.mjs` (not `./archive-browse.mjs` or any other form). All placeholder values in code blocks use `<angle-bracket>` notation consistent with the analog skills.
  </action>

  <verify>
    <automated>test -f .claude/skills/wm-archive-browse.md && echo "exists"</automated>
    <automated>grep -c "^# /wm-archive-browse" .claude/skills/wm-archive-browse.md</automated>
    <automated>grep -c "^## Steps" .claude/skills/wm-archive-browse.md</automated>
    <automated>grep -c "^### [0-9]\." .claude/skills/wm-archive-browse.md</automated>
    <automated>grep -c "Do NOT proceed" .claude/skills/wm-archive-browse.md</automated>
    <automated>grep -c "explicitly types" .claude/skills/wm-archive-browse.md</automated>
    <automated>grep -c "archive-browse.mjs" .claude/skills/wm-archive-browse.md</automated>
    <automated>grep -c "if_" .claude/skills/wm-archive-browse.md</automated>
    <automated>grep -c "^## Notes" .claude/skills/wm-archive-browse.md</automated>
  </verify>

  <acceptance_criteria>
    - File exists at `.claude/skills/wm-archive-browse.md`
    - First line is exactly `# /wm-archive-browse`
    - File contains `## Steps` section
    - `grep -c "^### [0-9]\." .claude/skills/wm-archive-browse.md` returns 6 (exactly six numbered steps)
    - Step 4 contains "Do NOT proceed" and "explicitly types \`y\`" (mandatory confirm gate per PATTERNS.md Skill Pattern confirm gate)
    - Step 2 contains a fenced bash code block with `node _scripts/archive-browse.mjs <slug>`
    - Step 5 contains a fenced bash code block with `node _scripts/archive-browse.mjs <slug> --capture <timestamp>`
    - File contains `if_` URL format string showing the toolbar-strip pattern (D-05, D-06)
    - File contains `## Notes` section as the last section
    - Notes section uses `- **\`key concept\`** —` format (backtick-wrapped key, em-dash, explanation)
    - File does NOT contain `## Prerequisites` or `## Usage` sections (not present in analog skills)
    - File does NOT contain any reference to `wm-gen-docs.md` or `wm-ingest.md` by name (skill is self-contained)
  </acceptance_criteria>

  <done>`.claude/skills/wm-archive-browse.md` exists with six numbered steps, a mandatory confirm gate before capture (per D-12), correct bash code blocks for timeline and capture invocations (per D-11/D-13), the if_ URL display pattern (per D-05), and a Notes section in the established skill format. ARCH-05 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator chat input → skill instructions | Operator provides slug/timestamp in chat; Claude passes these to the script subprocess |
| skill → archive-browse.mjs subprocess | Slug and timestamp entered by operator are interpolated into the bash command Claude runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | Operator-provided slug/timestamp passed to archive-browse.mjs subprocess | mitigate | Script (07-01) validates slug with `/^[a-z0-9-]+$/` and timestamp with `/^\d{14}$/` — skill relies on script-level validation; skill instructs Claude to pass args unmodified |
| T-07-06 | Tampering | Skill confirms capture before running — confirm gate prevents accidental or injected capture triggers | mitigate | Step 4 includes mandatory "Do NOT proceed until operator explicitly types y" language; gate cannot be bypassed |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Skill is a Markdown file; no package installs involved |
</threat_model>

<verification>
After task completes:

```bash
# Existence
test -f .claude/skills/wm-archive-browse.md && echo "OK"

# Structure checks
grep -c "^# /wm-archive-browse" .claude/skills/wm-archive-browse.md   # must be 1
grep -c "^## Steps" .claude/skills/wm-archive-browse.md               # must be 1
grep -c "^### [0-9]\." .claude/skills/wm-archive-browse.md            # must be 6
grep -c "explicitly types" .claude/skills/wm-archive-browse.md        # must be >= 1
grep -c "archive-browse.mjs" .claude/skills/wm-archive-browse.md      # must be >= 2
grep -c "if_" .claude/skills/wm-archive-browse.md                     # must be >= 1
grep -c "^## Notes" .claude/skills/wm-archive-browse.md               # must be 1
grep -c "Prerequisites\|## Usage" .claude/skills/wm-archive-browse.md # must be 0
```
</verification>

<success_criteria>
- `.claude/skills/wm-archive-browse.md` exists and matches the structural pattern of wm-gen-docs.md and wm-ingest.md
- Six numbered steps implement the D-11/D-12/D-13 interaction model exactly
- Mandatory confirm gate (Step 4) prevents automatic capture — operator must type y explicitly
- Step 2 and Step 5 bash code blocks use correct archive-browse.mjs invocation syntax
- if_ URL format is shown in Step 4 for operator browser inspection
- Notes section covers domain/wiring.json dependency, --limit override, namespaced output, internet requirement
- ARCH-05 satisfied
</success_criteria>

<output>
Create `.planning/phases/07-archive-module/07-02-SUMMARY.md` when done.
</output>
